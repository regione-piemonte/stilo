1        PACKAGE Dmpk_Ws AUTHID CURRENT_USER IS
2        
3        	/****************************************************		INFORMAZIONI GENERALI		*******************************************************/
4        	/************** Argomenti delle stored per il controllo della transazione:											*******/
5        	/************** FlgRollBckFullIn -> se 1 in caso di errore viene fatta la rollback completa (non a savepoint), 					*******/
6        	/************** 				altrimenti la rollback delle sole modifiche effettuate nella stored						*******/
7        	/************** 				ATTENZIONE: la rollback, di qualsiasi tipo, non riguarda la scrittura di eventuali log			*******/
8        	/************** FlgAutoCommitIn  -> se vale 1, dopo la rollback e qualunque sia l'esito (positivo o no), la stored esegue una commit finale *****/
9        	/************** Tutte le funzioni/procedure che NON hanno gli argomenti in input FlgRollBckFullIn e FlgAutoCommitIn 				*******/
10        	/************** NON ESEGUONO AL LORO INTERNO ALCUNA COMMIT nè ROLLBACK 												*******/
11        
12        	/************** Tutte le funzioni, tranne quelle da utilizzarsi nelle select, restituiscono 1 in caso di successo, altrimenti 0		*******/
13        	/************** Tali funzioni restituiscono, in caso di result 0, un n.ro, un contesto e un messaggio di errore:					*******/
14        	/************** il n.ro è: <0 se errore ORACLE non specificamente gestito 											*******/
15        	/**************		   da 1->1000 errore "grave" (SEVERE_ERROR) gestito											*******/
16        	/**************		   >1000 errore non grave gestito														*******/
17        	/************** il contesto indica il package e/o funzione/procedura in cui si è verificato l'errore							*******/
18        
19        	/***************** Funzione per creare una nuova unità documentaria (tramite Web Service)		**************/
20        	/***************** i cui metadati sono forniti in un xml conforme allo schema NewUD.xsd		**************/
21        	FUNCTION AddUD(
22        			CodIdConnectionTokenIn	IN		DMT_CONNECTION_TOKEN.CI_CONNECTION_TOKEN%TYPE,
23        															--(obblig.) Codice identificativo del token di connessione
24        			XMLIn				IN		CLOB,						--(obblig.) XML in input al Web service di creazione unità documentaria (secondo schema NewUD.xsd)
25        			DocAttachXMLOut		OUT		NOCOPY CLOB,				-- Lista con i dati per caricare (con step-version) nella repository i file associati all'unità documentaria e specificati come attach del messaggio SOAP(XML conforme a schema LISTA_STD.xsd)
26        															-- Per ogni attach vi è un tag Riga che contiene le seguenti colonne:
27        															-- 1: Valore fisso AV: indica il tipo di operazione da fare sul documento corrispondente all'attach
28        															-- 2: N.ro dell'attach del messaggio SOAP che corrisponde al file da caricare nella repository
29        															-- 3: Identificativo del documento creato in DB in corrispondenza dell'attachment
30        															-- 4: Nome del file da caricare (quello con cui mostrarlo e dalla cui estensione si ricava il formato)
31        															-- 5: (valori 1/0) Se 1 indica che è la versione è pubblicata, se 0 no
32        															-- 6: (valori 1/0) Se 1 indica che è la versione deriva da scansione, se 0 no
33        															-- 7: Codice che identifica la versione (n.ro versione alfanumerico, opzionale)
34        															-- 8: Note della versione
35        															-- 9: (valori 1/0) Se 1 indica che la versione deve essere sottoposta a OCR 
36        			RegNumDaRichASistEstOut	OUT		NOCOPY CLOB,				-- Lista con gli estremi delle registrazioni/numerazioni richieste da farsi dare da sistemi esterni (XML conforme a schema LISTA_STD.xsd) o da AURIGA stesso tramite API di RegistraUnitaDoc
37        															-- Per ogni registrazioni/numerazioni vi è un tag Riga che contiene le seguenti colonne:
38        															-- 1: Codice che indica la categoria di registrazione/numerazione (PG = Protocollo Generale, ecc)
39        															-- 2: Sigla che indica il registro di registrazione/numerazione
40        															-- 3: Anno di registrazione/numerazione se diverso da quello corrente
41        			XmlOut			OUT		NOCOPY CLOB,				-- XML con dati di output specifici del WS, restituito solo in caso di successo, conforme a schema Output_UD.xsd
42        			FlgRollBckFullIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se 1 in caso di errore viene fatta la rollback completa (non a savepoint), altrimenti la rollback delle sole modifiche effettuate nella stored
43        																--ATTENZIONE: La rollback, di qualsiasi tipo, non riguarda la scrittura di eventuali log
44        			FlgAutoCommitIn		IN 		PLS_INTEGER DEFAULT NULL,	--(valori 1/0/NULL) Se vale 1, dopo la rollback e qualunque sia l'esito (positivo o meno), la funzione esegue una commit finale
45        			ErrContextOut		OUT		VARCHAR2,					--Contesto (ovvero package e/o funzione/procedura) in cui si è verificato l'errore; se NULL è la funzione stessa
46        			ErrCodeOut			OUT		PLS_INTEGER,				--N.ro errore di uscita
47        			ErrMsgOut			OUT 		VARCHAR2,				--Messaggio d'errore
48        			AttachmentFileXMIn	IN		CLOB DEFAULT NULL		-- Lista XML conforme a schema ListaStd.xsd con le informazioni sui file (primario e allegati) dell'UD che devono essere già stati archiviati sullo storage definitivo
49        																-- In questa lista c'è una riga per ogni file passato in attach, mentre non ci sono righe per allegati senza file
50        																-- Se questa lista viene istanziata tutte le operazioni su file e allegati (anche senza file) vengono fatto all'interno di questa stored
51        																-- senza che debbano essere fatte ulteriori chiamate lato java: per questo DocAttachXMLOut in output NON viene del tutto considerata lato java
52        																-- Le colonne della lista sono:
53        																-- 1: DisplayName del file (il nome originale passato dal chiamante, NON rinominato per rendere coerente l'estensione con il formato riconosciuto)
54        																-- 2: URI del file salvato su archivio definitivo in notazione storageUtil
55        																-- 3: Nro di attachment con cui il file appare nella request secondo schema NEWUD.xsd
56        																--	  Nel caso di unico attachment zip che viene "spacchettato" come accade per il il SUE di ImpresaInUnGiorno il n.ro attach sarà sempre 1 per tutti i file dell'unico attachment zip
57        																-- 6: Dimensione (in bytes)
58        																-- 7: Impronta
59        																-- 8: algoritmo calcolo impronta
60        																-- 9: encoding di calcolo impronta: colonna
61        																-- 10: Flg file firmato (valori 1/0)
62        																-- 11: Mimetype
63        																-- 12: Firmatari(se più di uno separati da “;” )
64        																-- 13: Indicazione del tipo di firma (CAdES o PAdES)
65        																-- 14: Info di verifica della firma 
66        																-- 15: Data e ora delle marca se presente marca temporale valida (nel formato DD/MM/RRR HH24:MI:SS)
67        																-- 16: Tipo di marca temporale se presente
68        																-- 17: Informazioni di verifica della marca temporale se presente
69        																-- 22: Data e ora della firma digitale della busta crittografica più esterna, se presente (nel formato DD/MM/RRR HH24:MI:SS)
70        																-- 23: Flag di firma non valida alla data (valori 1/0/NULL) (la firma della busta crittografica più esterna)
71        																-- 24: Flag di manca temporale non valida alla data (valori 1/0/NULL)
72        																-- 25: Data emissione certificato firmatario (se più di uno separati da “;” )
73        																-- 26: Data scadenza certificato firmatario (se più di uno separati da “;” )
74        																-- 27: Indica se firma Qualifica (=Q) o Avanzata (=A) (se più di uno separate da “;” )
75        																-- 28: Cod. fiscali dei firmatari (se più di uno separati da “;” )
76        																-- 29: Impronta del file prima della firma per verifica che il file firmato sia coerente
77        																-- 30: Se 1 significa che è il file primario
78        			) RETURN PLS_INTEGER;								--Vale 1 se tutto è andato a buon fine, altrimenti 0
79        
80        
81        	/********* funzione richiamata dopo WS di AddUD con file inviati separati su ftp: serve ad effettuare la creazione dei documenti allegati se presenti e il versionamento dei file su DB *****/
82        	function AddFileUDConFileSeparati(
83        			IdUDIn					IN		DMT_UNITA_DOC.ID_UD%type,
84        																-- (obblig.) Id UD dell'unità documentaria creata con WS di AddUD con file inviati separatamente su ftp
85        			IndiceFileIn			IN		CLOB,				-- (obblig.) XML conforme a schema FileRegistrazioneInviatiSeparati.xsd che descrive i file ricevuti separatamente dalla chiamata alla AddUD su ftp
86        			InfoFileXMIn			IN		CLOB,				-- (obblig.) Lista XML conforme a schema FileRegistrazioneInviatiSeparati.xsd con le informazioni sui file (primario e allegati) dell'UD che devono essere già stati archiviati sullo storage definitivo
87        																-- Ogni riga è un file. I file devono essere indicati nell'esatto ordine in cui appaiono nel XML IndiceFileIn
88        																-- Le colonne della lista sono:
89        																-- 1: DisplayName del file (il nome originale passato dal chiamante, NON rinominato per rendere coerente l'estensione con il formato riconosciuto)
90        																-- 2: URI del file salvato su archivio definitivo in notazione storageUtil
91        																-- 6: Dimensione
92        																-- 7: impronta
93        																-- 8: algoritmo calcolo impronta
94        																-- 9: encoding di calcolo impronta: colonna
95        																-- 10: Flg file firmato (valori 1/0)
96        																-- 11: Mimetype
97        																-- 12: Firmatari(se più di uno separati da “;” )
98        																-- 13: Indicazione del tipo di firma (CAdES o PAdES)
99        																-- 14: Info di verifica della firma 
100        																-- 15: Data e ora delle marca se presente marca temporale valida (nel formato DD/MM/RRR HH24:MI:SS)
101        																-- 16: Tipo di marca temporale se presente
102        																-- 17: Informazioni di verifica della marca temporale se presente
103        																-- 22: Data e ora della firma digitale della busta crittografica più esterna, se presente (nel formato DD/MM/RRR HH24:MI:SS)
104        																-- 23: Flag di firma non valida alla data (valori 1/0/NULL) (la firma della busta crittografica più esterna)
105        																-- 24: Flag di manca temporale non valida alla data (valori 1/0/NULL)
106        																-- 25: Data emissione certificato firmatario (se più di uno separati da “;” )
107        																-- 26: Data scadenza certificato firmatario (se più di uno separati da “;” )
108        																-- 27: Indica se firma Qualifica (=Q) o Avanzata (=A) (se più di uno separate da “;” )
109        																-- 28: Cod. fiscali dei firmatari (se più di uno separati da “;” )
110        																-- 29: Impronta del file pre-firma													
111        			FlgRollBckFullIn		IN 		PLS_INTEGER DEFAULT NULL,		
112        																--(valori 1/0/NULL) Se 1 in caso di errore viene fatta la rollback completa (non a savepoint), altrimenti la rollback delle sole modifiche effettuate nella stored
113        																--ATTENZIONE: La rollback, di qualsiasi tipo, non riguarda la scrittura di eventuali log
114        			FlgAutoCommitIn			IN 		PLS_INTEGER DEFAULT NULL,	
115        																--(valori 1/0/NULL) Se vale 1, dopo la rollback e qualunque sia l'esito (positivo o meno), la funzione esegue una commit finale
116        			ErrContextOut			OUT		VARCHAR2,			-- Contesto (ovvero package e/o funzione/procedura) in cui si è verificato l'errore; se NULL è la funzione stessa
117        			ErrCodeOut				OUT		PLS_INTEGER,		-- N.ro errore di uscita
118        			ErrMsgOut				OUT 	VARCHAR2			-- Messaggio d'errore
119        			) RETURN PLS_INTEGER;								-- Vale 1 se tutto è andato a buon fine, altrimenti 0
120        
121        	/********* funzione da richiamare prima della UpdUD per ottenere i file dell'UD di cui ricontrollare firme e marche prima di procedere alla UpdUD *****/
122        	function GetFileUDToUpdXRectrl(
123        			CodIdConnectionTokenIn	IN		DMT_CONNECTION_TOKEN.CI_CONNECTION_TOKEN%TYPE,
124        																--(obblig.) Codice identificativo del token di connessione
125        			XMLIn					IN		CLOB,				--(obblig.) XML in input al Web service di aggiornamento unità documentaria (secondo schema UDDaAgg.xsd)
126        			ListaDocRectrlIn		OUT		NOCOPY CLOB,		-- Lista con gli estremi dei documenti di cui ricontrollare firme e marche
127        																-- 1: (obbligatorio) DisplayName del file
128        																-- 2: (valorizzaro solo se colonna 28 è vuota) URI del file in notazione storageUtil
129        																-- 25: (obbligatorio) ID del documento di cui ri-controllare il file. -1 per indicare che è un documento nuovo
130        																-- 26: (obbligatorio) 0 se primario, altrimenti il numero di allegato. Se è un NuovoAllegatoUD restituisco -1
131        																-- 27: (obbligatorio) Nro di versione del file da ricontrollare. Se è un file nuovo passato in attach restituisco -1
132        																-- 28: NroAttachmentAssociato se è un file nuovo passato in attach
133        																-- 29: Data e ora a cui controllare la validità della firma (in formato FMT_STD_TIMESTAMP)
134        			ErrContextOut		OUT		VARCHAR2,				--Contesto (ovvero package e/o funzione/procedura) in cui si è verificato l'errore; se NULL è la funzione stessa
135        			ErrCodeOut			OUT		PLS_INTEGER,			--N.ro errore di uscita
136        			ErrMsgOut			OUT 	VARCHAR2				--Messaggio d'errore
137        			)RETURN PLS_INTEGER;								--Vale 1 se il recupero dei file è andato a buon fine, altrimenti 0
138        
139        	/***************** Funzione per aggiornare un'unità documentaria (tramite Web Service)				**************/
140        	/***************** i metadati da aggiornare sono forniti in un xml conforme allo schema UDDaAgg.xsd		**************/
141        	FUNCTION UpdUD(
142        			CodIdConnectionTokenIn	IN		DMT_CONNECTION_TOKEN.CI_CONNECTION_TOKEN%TYPE,
143        															--(obblig.) Codice identificativo del token di connessione
144        			XMLIn				IN		CLOB,				--(obblig.) XML in input al Web service di aggiornamento unità documentaria (secondo schema UDDaAgg.xsd)
145        			OperVsDocXMLOut		OUT		NOCOPY CLOB,		-- Lista con i dati su come aggiornare nella repository i documenti associati all'unità documentaria (XML conforme a schema LISTA_STD.xsd)
146        															-- Per ogni operazione da fare su un documento vi è un tag Riga che contiene le seguenti colonne:
147        															-- 1: Tipo di operazione da fare sul documento; valori possibili:
148        															--		AV = Step-version, ovvero aggiunta di nuova versione elettronica (anche la prima)
149        															--		MM = Modifica dei metadati del documento
150        															--		DV = Cancellazione dell'ultima versione elettronica valida e visibile all'utente
151        															--		DD = Cancellazione del documento
152        															--		SV = Sostituzione di versione
153        															--		MV = Modifica metadati di versione
154        															-- 2: N.ro dell'attach del messaggio SOAP che corrisponde al file da caricare nella repository (valorizzato solo se il tipo di operazione è AV o SV)
155        															-- 3: Identificativo del documento
156        															-- 4: Nome del file della versione (valorizzato solo se il tipo di operazione è AV o SV o MV); è il nome con cui mostrarlo e dalla cui estensione si ricava il formato)
157        															-- 5: (valori 1/0) Se 1 indica che è la versione è pubblicata, se 0 no (valorizzato solo se il tipo di operazione è AV o SV o MV)
158        															-- 6: (valori 1/0) Se 1 indica che è la versione deriva da scansione, se 0 no (valorizzato solo se il tipo di operazione è AV o SV o MV)
159        															-- 7: Codice che identifica la versione (n.ro versione alfanumerico, opzionale) (valorizzato solo se il tipo di operazione è AV o SV o MV)
160        															-- 8: Note della versione (valorizzato solo se il tipo di operazione è AV o SV o MV)
161        															-- 9: (valori 1/0) Se 1 indica che la versione deve essere sottoposta a OCR 
162        			RegNumDaRichASistEstOut	OUT		NOCOPY CLOB,				-- Lista con gli estremi delle registrazioni/numerazioni richieste da farsi dare da sistemi esterni (XML conforme a schema LISTA_STD.xsd) o da AURIGA stesso tramite API di RegistraUnitaDoc
163        															-- Per ogni registrazioni/numerazioni vi è un tag Riga che contiene le seguenti colonne:
164        															-- 1: Codice che indica la categoria di registrazione/numerazione (PG = Protocollo Generale, ecc)
165        															-- 2: Sigla che indica il registro di registrazione/numerazione
166        															-- 3: Anno di registrazione/numerazione se diverso da quello corrente
167        			XmlOut			OUT		NOCOPY CLOB,				-- XML con dati di output specifici del WS, restituito solo in caso di successo, conforme a schema Output_UD.xsd
168        			FlgRollBckFullIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se 1 in caso di errore viene fatta la rollback completa (non a savepoint), altrimenti la rollback delle sole modifiche effettuate nella stored
169        																--ATTENZIONE: La rollback, di qualsiasi tipo, non riguarda la scrittura di eventuali log
170        			FlgAutoCommitIn		IN 		PLS_INTEGER DEFAULT NULL,--(valori 1/0/NULL) Se vale 1, dopo la rollback e qualunque sia l'esito (positivo o meno), la funzione esegue una commit finale
171        			ErrContextOut		OUT		VARCHAR2,				--Contesto (ovvero package e/o funzione/procedura) in cui si è verificato l'errore; se NULL è la funzione stessa
172        			ErrCodeOut			OUT		PLS_INTEGER,			--N.ro errore di uscita
173        			ErrMsgOut			OUT 	VARCHAR2,				--Messaggio d'errore
174        			AttachmentFileXMIn	IN		CLOB DEFAULT NULL,		-- Lista XML conforme a schema ListaStd.xsd con le informazioni sui file nuovi o cambiati (primario e allegati) dell'UD che devono essere già stati archiviati sullo storage definitivo
175        																-- In questa lista c'è una riga per ogni file passato in attach, mentre non ci sono righe per file eliminati o allegati eliminati o allegati senza file
176        																-- Se questa lista viene istanziata tutte le operazioni su file e allegati (anche senza file) vengono fatto all'interno di questa stored
177        																-- senza che debbano essere fatte ulteriori chiamate lato java: per questo OperVsDocXMLOut in output NON viene del tutto considerata lato java
178        																-- Le colonne della lista sono:
179        																-- 1: DisplayName del file (il nome originale passato dal chiamante, NON rinominato per rendere coerente l'estensione con il formato riconosciuto)
180        																-- 2: URI del file salvato su archivio definitivo in notazione storageUtil
181        																-- 3: Nro di attachment con cui il file appare nella request secondo schema NEWUD.xsd
182        																--	  Nel caso di unico attachment zip che viene "spacchettato" come accade per il il SUE di ImpresaInUnGiorno il n.ro attach sarà sempre 1 per tutti i file dell'unico attachment zip
183        																-- 6: Dimensione (in bytes)
184        																-- 7: Impronta
185        																-- 8: algoritmo calcolo impronta
186        																-- 9: encoding di calcolo impronta: colonna
187        																-- 10: Flg file firmato (valori 1/0)
188        																-- 11: Mimetype
189        																-- 12: Firmatari(se più di uno separati da “;” )
190        																-- 13: Indicazione del tipo di firma (CAdES o PAdES)
191        																-- 14: Info di verifica della firma 
192        																-- 15: Data e ora delle marca se presente marca temporale valida (nel formato DD/MM/RRR HH24:MI:SS)
193        																-- 16: Tipo di marca temporale se presente
194        																-- 17: Informazioni di verifica della marca temporale se presente
195        																-- 22: Data e ora della firma digitale della busta crittografica più esterna, se presente (nel formato DD/MM/RRR HH24:MI:SS)
196        																-- 23: Flag di firma non valida alla data (valori 1/0/NULL) (la firma della busta crittografica più esterna)
197        																-- 24: Flag di manca temporale non valida alla data (valori 1/0/NULL)
198        																-- 25: Data emissione certificato firmatario (se più di uno separati da “;” )
199        																-- 26: Data scadenza certificato firmatario (se più di uno separati da “;” )
200        																-- 27: Indica se firma Qualifica (=Q) o Avanzata (=A) (se più di uno separate da “;” )
201        																-- 28: Cod. fiscali dei firmatari (se più di uno separati da “;” )
202        																-- 29: Impronta del file prima della firma per verifica che il file firmato sia coerente
203        																-- 30: Se 1 significa che è il file primario
204        			ListaDocRectrlIn	IN		CLOB DEFAULT NULL		-- Lista XML con i file primario e allegati già archiviati e ricontrollati in sede di update UD
205        																-- Possono esserci anche file da caricare ex-novo come primario o allegati. Nel caso col 25 e 27 sono settate a -1
206        																-- Ogni riga corrisponde ad un documento-file ricontrolato e contiene le colonne:
207        																-- 1: DisplayName del file
208        																-- 10: Flg file firmato (valori 1/0)
209        																-- 23: Flag di firma non valida alla data (valori 1/0/NULL) (la firma della busta crittografica più esterna)
210        																-- 25: (obbligatorio) ID del documento di cui ri-controllare il file. -1 per indicare che è un documento nuovo
211        																-- 26: (obbligatorio) 0 se primario, altrimenti il numero di allegato
212        																--		Se è un NuovoAllegatoUD con NroAttachmentAssociato valorizzato (ovvero passato in attach) settare -1
213        																-- 27: (obbligatorio) Nro di versione del file da ricontrollare. -1 per indicare che è un file nuovo passato in attach.
214        																-- 28: NroAttachmentAssociato in caso di file passato in attach al servizio
215        			)RETURN PLS_INTEGER;								--Vale 1 se tutto è andato a buon fine, altrimenti 0
216        
217        
218        	/***************** Funzione per ottenere xml di response di una AddUd o UpdUD (tramite Web Service)		**************/
219        	FUNCTION GetResponseAddUpdUD(
220        			CodIdConnectionTokenIn	IN		DMT_CONNECTION_TOKEN.CI_CONNECTION_TOKEN%TYPE,
221        			IdUDIn			IN		INTEGER,					-- Id. UD dell'UD creata o aggiornata
222        			XmlOut			OUT		NOCOPY CLOB,				-- XML con dati di output specifici del WS, restituito solo in caso di successo, conforme a schema Output_UD.xsd
223        			FlgRollBckFullIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se 1 in caso di errore viene fatta la rollback completa (non a savepoint), altrimenti la rollback delle sole modifiche effettuate nella stored
224        																--ATTENZIONE: La rollback, di qualsiasi tipo, non riguarda la scrittura di eventuali log
225        			FlgAutoCommitIn		IN 		PLS_INTEGER DEFAULT NULL,	--(valori 1/0/NULL) Se vale 1, dopo la rollback e qualunque sia l'esito (positivo o meno), la funzione esegue una commit finale
226        			ErrContextOut		OUT		VARCHAR2,					--Contesto (ovvero package e/o funzione/procedura) in cui si è verificato l'errore; se NULL è la funzione stessa
227        			ErrCodeOut			OUT		PLS_INTEGER,				--N.ro errore di uscita
228        			ErrMsgOut			OUT 		VARCHAR2				--Messaggio d'errore
229        			) RETURN PLS_INTEGER;								--Vale 1 se tutto è andato a buon fine, altrimenti 0
230        
231        
232        	/***************** Funzione per estrarre un singolo file legato ad un'unità documentaria (tramite Web Service) 			**************/
233        	/***************** gli estremi del file da estrarre sono forniti in un xml conforme allo schema FileUDToExtract.xsd		**************/
234        	FUNCTION ExtractFileUD(
235        			CodIdConnectionTokenIn	IN		DMT_CONNECTION_TOKEN.CI_CONNECTION_TOKEN%TYPE,
236        															--(obblig.) Codice identificativo del token di connessione
237        			XMLIn				IN		CLOB,						--(obblig.) XML in input al Web service di estrazione file di un'unità documentaria (secondo schema FileUDToExtract.xsd)
238        			XmlOut			OUT		NOCOPY CLOB,				-- XML con dati di output specifici del WS, restituito solo in caso di successo, conforme a schema Output_FileUDToExtract.xsd
239        			IdDocToExtractOut		OUT		INTEGER,					-- ID_DOC del documento di cui estrarre la versione
240        			NroVerToExtractOut	OUT		INTEGER,					-- N.ro della versione da estrarre
241        			FlgVerTimbrataOut		OUT     INTEGER,				-- Se 1 indica che la versione archivata è già timbrata con la segnatura di registazione
242        			FlgRollBckFullIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se 1 in caso di errore viene fatta la rollback completa (non a savepoint), altrimenti la rollback delle sole modifiche effettuate nella stored
243        															--ATTENZIONE: La rollback, di qualsiasi tipo, non riguarda la scrittura di eventuali log
244        			FlgAutoCommitIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se vale 1, dopo la rollback e qualunque sia l'esito (positivo o meno), la funzione esegue una commit finale
245        			ErrContextOut		OUT		VARCHAR2,					--Contesto (ovvero package e/o funzione/procedura) in cui si è verificato l'errore; se NULL è la funzione stessa
246        			ErrCodeOut			OUT		PLS_INTEGER,				--N.ro errore di uscita
247        			ErrMsgOut			OUT 		VARCHAR2)					--Messaggio d'errore
248        			RETURN PLS_INTEGER;									--Vale 1 se tutto è andato a buon fine, altrimenti 0
249        
250        	---- funzione per estrarre le info utili al rendering pdf di un documento non nativamente pdf (es. Fattura elettronica o altro file xml)
251        	FUNCTION GetInfoRenderingPdfDoc(
252        			CodIdConnectionTokenIn		IN		DMT_CONNECTION_TOKEN.CI_CONNECTION_TOKEN%TYPE,
253        																--(obblig.) Codice identificativo del token di connessione
254        			IdDocIn						IN		INTEGER,		--(obblig.) Id. del documento di cui estrarre info per rendering PDF
255        			FlgFatturaElettronicaOut	OUT     INTEGER,		-- Se 1 indica che il documento è una fattura elettronica
256        			NomeModelloOut				OUT     VARCHAR2,		-- Nome del modello da usare per rendering pdf. Modello di tipo freemarker
257        			IdModelloOut				OUT     DMT_MODELLI_DOC.ID_MODELLO%type,
258        																-- Id. del modello da usare per rendering pdf. Modello di tipo freemarker
259        			UriModelloOut				OUT     VARCHAR2,		-- URI in notazione storageUtil del modello da usare per rendering pdf
260        			NomeDocTypeOut				OUT     VARCHAR2,		-- Nome del tipo di documento
261        			IdDocTypeOut				OUT     INTEGER,		-- Id. del tipo di documento
262        			ErrContextOut		OUT		VARCHAR2,				--Contesto (ovvero package e/o funzione/procedura) in cui si è verificato l'errore; se NULL è la funzione stessa
263        			ErrCodeOut			OUT		PLS_INTEGER,			--N.ro errore di uscita
264        			ErrMsgOut			OUT 		VARCHAR2)			--Messaggio d'errore
265        			RETURN PLS_INTEGER;									--Vale 1 se tutto è andato a buon fine e se si tratta di fattura elettronica o documento che ha modello associato, altrimenti 0
266        
267        	/***************** Funzione per estrarre tutti i file (visibili) legati ad un'unità documentaria (tramite Web Service) 					**************/
268        	/***************** gli estremi dell'unità doc. di cui estrarre i file sono forniti in un xml conforme allo schema EstremiXIdentificazioneUD.xsd	**************/
269        	FUNCTION ExtractFilesUD(
270        			CodIdConnectionTokenIn	IN		DMT_CONNECTION_TOKEN.CI_CONNECTION_TOKEN%TYPE,
271        															--(obblig.) Codice identificativo del token di connessione
272        			XMLIn				IN		CLOB,						--(obblig.) XML in input al Web service di estrazione files di un'unità documentaria (secondo schema EstremiXIdentificazioneUD.xsd)
273        			XmlOut			OUT		NOCOPY CLOB,				-- XML con dati di output specifici del WS, restituito solo in caso di successo, conforme a schema Output_FilesUD.xsd
274        			VerDocToExtractTabOut	OUT		NOCOPY CLOB,				-- Lista con gli id. dei documenti da estrarre (XML conforme a schema LISTA_STD.xsd)
275        															-- Ogni documento è un tag Riga che contiene le seguenti colonne:
276        															-- 1: Id. del documento
277        															-- 2: N.ro della versione da estrarre (l'ultima valida visibile all'utente)
278        															-- 3: Nome file (quando il servizio viene chiamato per restituire i file in uno zip)
279        															-- 4: URI del file in notazione storageUtil
280        			FlgRollBckFullIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se 1 in caso di errore viene fatta la rollback completa (non a savepoint), altrimenti la rollback delle sole modifiche effettuate nella stored
281        															--ATTENZIONE: La rollback, di qualsiasi tipo, non riguarda la scrittura di eventuali log
282        			FlgAutoCommitIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se vale 1, dopo la rollback e qualunque sia l'esito (positivo o meno), la funzione esegue una commit finale
283        			ErrContextOut		OUT		VARCHAR2,					--Contesto (ovvero package e/o funzione/procedura) in cui si è verificato l'errore; se NULL è la funzione stessa
284        			ErrCodeOut			OUT		PLS_INTEGER,				--N.ro errore di uscita
285        			ErrMsgOut			OUT 		VARCHAR2)					--Messaggio d'errore
286        			RETURN PLS_INTEGER;									--Vale 1 se tutto è andato a buon fine, altrimenti 0
287        
288        	/***************** Funzione che a partire da un xml conforme allo schema EstremiXIdentificazioneDoc.xsd ricava il documento su cui lavorare 				******/
289        	/***************** serve per i WS di lock, unlock e check-out. Nel caso del check-out restituisce anche i dati del documento checked-out e del file estratto 	******/
290        	FUNCTION LockCheckOut(
291        			CodIdConnectionTokenIn	IN		DMT_CONNECTION_TOKEN.CI_CONNECTION_TOKEN%TYPE,
292        															--(obblig.) Codice identificativo del token di connessione
293        			FlgTipoWSIn			IN		VARCHAR2,					--(obblig.) Tipo di Web Service che richiama la funzione. Valori ammessi:
294        															-- L	= Lock
295        															-- U	= Unlock
296        															-- C	= Check-out
297        			XMLIn				IN		CLOB,						--(obblig.) XML con tracciato conforme a schema EstremiXIdentificazioneDoc.xsd
298        			IdDocOut			OUT		DMT_DOCUMENTS.ID_DOC%TYPE,		-- Id. del documento ricavato dall'XML in input
299        			XMLOut			OUT		NOCOPY CLOB,				-- XML di output conforme allo schema Output_FileCheckedOut.xsd.
300        															-- E' valorizzato solo se FlgTipoWSIn=C e in caso di esito positivo
301        			FlgRollBckFullIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se 1 in caso di errore viene fatta la rollback completa (non a savepoint), altrimenti la rollback delle sole modifiche effettuate nella stored
302        															--ATTENZIONE: La rollback, di qualsiasi tipo, non riguarda la scrittura di eventuali log
303        			FlgAutoCommitIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se vale 1, dopo la rollback e qualunque sia l'esito (positivo o meno), la funzione esegue una commit finale
304        			ErrContextOut		OUT		VARCHAR2,					--Contesto (ovvero package e/o funzione/procedura) in cui si è verificato l'errore; se NULL è la funzione stessa
305        			ErrCodeOut			OUT		PLS_INTEGER,				--N.ro errore di uscita
306        			ErrMsgOut			OUT 		VARCHAR2)					--Messaggio d'errore
307        			RETURN PLS_INTEGER;									--Vale 1 se tutto è andato a buon fine, altrimenti 0
308        
309        	/***************** Funzione da richiamare nel WS che fa check-in/step-version di un documento 	******/
310        	/***************** ovvero nel WS che crea una nuova/prima versione di un documento			******/
311        	FUNCTION CheckIn(
312        			CodIdConnectionTokenIn	IN		DMT_CONNECTION_TOKEN.CI_CONNECTION_TOKEN%TYPE,
313        															--(obblig.) Codice identificativo del token di connessione
314        			XMLIn				IN		CLOB,						--(obblig.) XML con tracciato conforme a schema CheckIn.xsd
315        			IdDocOut			OUT		DMT_DOCUMENTS.ID_DOC%TYPE,		-- Id. del documento di cui fare check-in
316        			NroVersioneEstrattaOut	OUT		PLS_INTEGER,				-- N.ro progressivo della versione checked-out/estratta dall'utente e che lui ritiene di aggiornare;
317        															-- valorizzandola si richiede che venga controllato che essa coincida con l'attuale ultima versione del documento
318        															-- Dunque se è valorizzato va fatta check-in, se no step-versione
319        			FlgVerificaFirmaFileOut	OUT		PLS_INTEGER,				-- (valori 1/0) Se 1 indica che deve essere verificata l'eventuale firma digitale del file che deve dar luogo alla nuova versione
320        			AttributiVerXMLOut	OUT		NOCOPY CLOB,				-- XML contenente nomi e valori degli attributi della nuova versione elettronica (conforme allo schema SezioneCache.xsd)
321        			FlgRollBckFullIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se 1 in caso di errore viene fatta la rollback completa (non a savepoint), altrimenti la rollback delle sole modifiche effettuate nella stored
322        															--ATTENZIONE: La rollback, di qualsiasi tipo, non riguarda la scrittura di eventuali log
323        			FlgAutoCommitIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se vale 1, dopo la rollback e qualunque sia l'esito (positivo o meno), la funzione esegue una commit finale
324        			ErrContextOut		OUT		VARCHAR2,					--Contesto (ovvero package e/o funzione/procedura) in cui si è verificato l'errore; se NULL è la funzione stessa
325        			ErrCodeOut			OUT		PLS_INTEGER,				--N.ro errore di uscita
326        			ErrMsgOut			OUT 		VARCHAR2)					--Messaggio d'errore
327        			RETURN PLS_INTEGER;									--Vale 1 se tutto è andato a buon fine, altrimenti 0
328        
329        	/***************** Funzione per estrarre i metadati di un'unità documentaria  												**************/
330        	/***************** gli estremi dell'unità doc. di cui estrarre i metadati sono forniti in un xml conforme allo schema EstremiXIdentificazioneUD.xsd	**************/
331        	FUNCTION GetDatiUD(
332        			CodIdConnectionTokenIn	IN		DMT_CONNECTION_TOKEN.CI_CONNECTION_TOKEN%TYPE,
333        															--(obblig.) Codice identificativo del token di connessione
334        			XMLIn				IN		CLOB,						--(obblig.) XML in input al Web service di estrazione metadati di un'unità documentaria (secondo schema EstremiXIdentificazioneUD.xsd)
335        			XmlOut			OUT		NOCOPY CLOB,				-- XML con dati di output specifici del WS, restituito solo in caso di successo, conforme a schema Output_DatiUD.xsd
336        			FlgRollBckFullIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se 1 in caso di errore viene fatta la rollback completa (non a savepoint), altrimenti la rollback delle sole modifiche effettuate nella stored
337        															--ATTENZIONE: La rollback, di qualsiasi tipo, non riguarda la scrittura di eventuali log
338        			FlgAutoCommitIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se vale 1, dopo la rollback e qualunque sia l'esito (positivo o meno), la funzione esegue una commit finale
339        			ErrContextOut		OUT		VARCHAR2,					--Contesto (ovvero package e/o funzione/procedura) in cui si è verificato l'errore; se NULL è la funzione stessa
340        			ErrCodeOut			OUT		PLS_INTEGER,				--N.ro errore di uscita
341        			ErrMsgOut			OUT 		VARCHAR2)					--Messaggio d'errore
342        			RETURN PLS_INTEGER;									--Vale 1 se tutto è andato a buon fine, altrimenti 0
343        
344        	/***************** Funzione per estrarre i metadati degli invvi e-email di un'unità documentaria  							**************/
345        	/***************** In attach al servizio possono essere restituite anche mail inviate e/o loro ricevute/notifiche collegate	**************/
346        	FUNCTION GetDatiInviiMailUD(
347        			CodIdConnectionTokenIn	IN		DMT_CONNECTION_TOKEN.CI_CONNECTION_TOKEN%TYPE,
348        																	--(obblig.) Codice identificativo del token di connessione
349        			XMLIn				IN		CLOB,						--(obblig.) XML in input al Web service (secondo schema Request_GetDatiInviiMailUD.xsd)
350        			XmlOut				OUT		NOCOPY CLOB,				-- XML con dati di output specifici del WS, restituito solo in caso di successo, conforme a schema Respose_GetDatiInviiMailUD.xsd
351        			ListaAttachOut		OUT		NOCOPY CLOB,				-- Lista xml con gli attach che il WS deve restituire oltre il primo con l'xml di risposta
352        																	-- Ogni record è un'attach e contiene le colonne:
353        																	-- 1) Nro di attach
354        																	-- 2) URI del file da mettere in attach in notazione storageUtil
355        			FlgRollBckFullIn	IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se 1 in caso di errore viene fatta la rollback completa (non a savepoint), altrimenti la rollback delle sole modifiche effettuate nella stored
356        															--ATTENZIONE: La rollback, di qualsiasi tipo, non riguarda la scrittura di eventuali log
357        			FlgAutoCommitIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se vale 1, dopo la rollback e qualunque sia l'esito (positivo o meno), la funzione esegue una commit finale
358        			ErrContextOut		OUT		VARCHAR2,					--Contesto (ovvero package e/o funzione/procedura) in cui si è verificato l'errore; se NULL è la funzione stessa
359        			ErrCodeOut			OUT		PLS_INTEGER,				--N.ro errore di uscita
360        			ErrMsgOut			OUT 		VARCHAR2)					--Messaggio d'errore
361        			RETURN PLS_INTEGER;									--Vale 1 se tutto è andato a buon fine, altrimenti 0
362        
363        	/****************** Funzione per trovare l'id. di un unità documentaria a partire da un XML conforme allo schema EstremiXIdentificazioneUD.xsd	**************/
364        	FUNCTION LeggiEstremiXIdentificazioneUD(
365        			CodIdConnectionTokenIn	IN		DMT_CONNECTION_TOKEN.CI_CONNECTION_TOKEN%TYPE,
366        															--(obblig.) Codice identificativo del token di connessione
367        			XMLIn				IN		CLOB,						--(obblig.) XML conforme a schema EstremiXIdentificazioneUD.xsd)
368        			IdUDOut			OUT		DMT_UNITA_DOC.ID_UD%TYPE,		--Identificativo dell'unità documentaria trovata a partire dall'XML
369        			ErrContextOut		OUT		VARCHAR2,					--Contesto (ovvero package e/o funzione/procedura) in cui si è verificato l'errore; se NULL è la funzione stessa
370        			ErrCodeOut			OUT		PLS_INTEGER,				--N.ro errore di uscita
371        			ErrMsgOut			OUT 		VARCHAR2)					--Messaggio d'errore
372        			RETURN PLS_INTEGER;									--Vale 1 se dall'XML in input l'unità documentaria è stata individuata univocamente, altrimenti 0
373        
374        	/************ Funzione per cancellare logicamente o fisicamente una versione eletronica di documento											**********/
375        	/************ gli estremi della versione da cancellare e del tipo di cancellazione (fisica o logica) sono forniti in un xml conforme a schema DeleteVerDoc.xsd 	**********/
376        	FUNCTION DelVerDoc(
377        			CodIdConnectionTokenIn	IN		DMT_CONNECTION_TOKEN.CI_CONNECTION_TOKEN%TYPE,
378        															--(obblig.) Codice identificativo del token di connessione
379        			XMLIn				IN		CLOB,						--(obblig.) XML in input al Web service di cancellazione versione (secondo schema DeleteVerDoc.xsd)
380        			IdDocOut			OUT		DMT_DOCUMENTS.ID_DOC%TYPE,		--Identificativo del documento cui è relativa la versione da cancellare
381        			NroProgrVerOut		OUT		PLS_INTEGER,				--N.ro progr. della versione da cancellare
382        			FlgTipoDelOut		OUT		VARCHAR2,					--(valori F/L) Tipo di cancellazione richiesta: F = Fisica, L = Logica
383        			FlgRollBckFullIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se 1 in caso di errore viene fatta la rollback completa (non a savepoint), altrimenti la rollback delle sole modifiche effettuate nella stored
384        															--ATTENZIONE: La rollback, di qualsiasi tipo, non riguarda la scrittura di eventuali log
385        			FlgAutoCommitIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se vale 1, dopo la rollback e qualunque sia l'esito (positivo o meno), la funzione esegue una commit finale
386        			ErrContextOut		OUT		VARCHAR2,					--Contesto (ovvero package e/o funzione/procedura) in cui si è verificato l'errore; se NULL è la funzione stessa
387        			ErrCodeOut			OUT		PLS_INTEGER,				--N.ro errore di uscita
388        			ErrMsgOut			OUT 		VARCHAR2)					--Messaggio d'errore
389        			RETURN PLS_INTEGER;									--Vale 1 se tutto è andato a buon fine, altrimenti 0
390        
391        	/************ Funzione per cancellare logicamente o fisicamente un documento. Se il documento specificato è un primario la cancellazione da fare è dell'intera unità documentaria	**********/
392        	/************ gli estremi del documento da cancellare e del tipo di cancellazione (fisica o logica) sono forniti in un xml conforme a schema DeleteDoc.xsd 					**********/
393        	FUNCTION DelDoc(
394        			CodIdConnectionTokenIn	IN		DMT_CONNECTION_TOKEN.CI_CONNECTION_TOKEN%TYPE,
395        															--(obblig.) Codice identificativo del token di connessione
396        			XMLIn				IN		CLOB,						--(obblig.) XML in input al Web service di cancellazione versione (secondo schema DeleteDoc.xsd)
397        			FlgTipoTargetOut		OUT		VARCHAR2,					--(valori U/D) Indica cosa va cancellato: U = Unità documentaria; D = Documento
398        			IdUDDocOut			OUT		INTEGER,					--Identificativo del documento o unità documentaria da cancellare
399        			FlgTipoDelOut		OUT		VARCHAR2,					--(valori F/L) Tipo di cancellazione richiesta: F = Fisica, L = Logica
400        			FlgRollBckFullIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se 1 in caso di errore viene fatta la rollback completa (non a savepoint), altrimenti la rollback delle sole modifiche effettuate nella stored
401        															--ATTENZIONE: La rollback, di qualsiasi tipo, non riguarda la scrittura di eventuali log
402        			FlgAutoCommitIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se vale 1, dopo la rollback e qualunque sia l'esito (positivo o meno), la funzione esegue una commit finale
403        			ErrContextOut		OUT		VARCHAR2,					--Contesto (ovvero package e/o funzione/procedura) in cui si è verificato l'errore; se NULL è la funzione stessa
404        			ErrCodeOut			OUT		PLS_INTEGER,				--N.ro errore di uscita
405        			ErrMsgOut			OUT 		VARCHAR2)					--Messaggio d'errore
406        			RETURN PLS_INTEGER;									--Vale 1 se tutto è andato a buon fine, altrimenti 0
407        
408        	/************ Funzione per cancellare logicamente o fisicamente un'unità documentaria. 														**********/
409        	/************ gli estremi dell'unità documentaria da cancellare e del tipo di cancellazione (fisica o logica) sono forniti in un xml conforme a schema DeleteUD.xsd  	**********/
410        	FUNCTION DelUD(
411        			CodIdConnectionTokenIn	IN		DMT_CONNECTION_TOKEN.CI_CONNECTION_TOKEN%TYPE,
412        															--(obblig.) Codice identificativo del token di connessione
413        			XMLIn				IN		CLOB,						--(obblig.) XML in input al Web service di cancellazione versione (secondo schema DeleteUD.xsd)
414        			IdUDOut			OUT		DMT_UNITA_DOC.ID_UD%TYPE,		--Identificativo dell'unità documentaria da cancellare
415        			FlgTipoDelOut		OUT		VARCHAR2,					--(valori F/L) Tipo di cancellazione richiesta: F = Fisica, L = Logica
416        			FlgRollBckFullIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se 1 in caso di errore viene fatta la rollback completa (non a savepoint), altrimenti la rollback delle sole modifiche effettuate nella stored
417        															--ATTENZIONE: La rollback, di qualsiasi tipo, non riguarda la scrittura di eventuali log
418        			FlgAutoCommitIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se vale 1, dopo la rollback e qualunque sia l'esito (positivo o meno), la funzione esegue una commit finale
419        			ErrContextOut		OUT		VARCHAR2,					--Contesto (ovvero package e/o funzione/procedura) in cui si è verificato l'errore; se NULL è la funzione stessa
420        			ErrCodeOut			OUT		PLS_INTEGER,				--N.ro errore di uscita
421        			ErrMsgOut			OUT 		VARCHAR2)					--Messaggio d'errore
422        			RETURN PLS_INTEGER;									--Vale 1 se tutto è andato a buon fine, altrimenti 0
423        
424        	/***************** Funzione per creare una nuovo folder/libreria (tramite Web Service)		**************/
425        	/***************** i cui metadati sono forniti in un xml conforme allo schema NewFolder.xsd	**************/
426        	FUNCTION AddFolder(
427        			CodIdConnectionTokenIn	IN		DMT_CONNECTION_TOKEN.CI_CONNECTION_TOKEN%TYPE,
428        															--(obblig.) Codice identificativo del token di connessione
429        			XMLIn				IN		CLOB,						--(obblig.) XML in input al Web service di creazione folder (secondo schema NewFolder.xsd)
430        			XmlOut			OUT		NOCOPY CLOB,				-- XML (secondo schema SezioneCache.xsd) con tutti gli attributi del folder da passare all'API di creazione folder
431        			FlgRollBckFullIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se 1 in caso di errore viene fatta la rollback completa (non a savepoint), altrimenti la rollback delle sole modifiche effettuate nella stored
432        															--ATTENZIONE: La rollback, di qualsiasi tipo, non riguarda la scrittura di eventuali log
433        			FlgAutoCommitIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se vale 1, dopo la rollback e qualunque sia l'esito (positivo o meno), la funzione esegue una commit finale
434        			ErrContextOut		OUT		VARCHAR2,					--Contesto (ovvero package e/o funzione/procedura) in cui si è verificato l'errore; se NULL è la funzione stessa
435        			ErrCodeOut			OUT		PLS_INTEGER,				--N.ro errore di uscita
436        			ErrMsgOut			OUT 		VARCHAR2)					--Messaggio d'errore
437        			RETURN PLS_INTEGER;									--Vale 1 se tutto è andato a buon fine, altrimenti 0
438        
439        
440        	/***************** Funzione che a partire da IdFolder restituisce xml con estremi del folder				******/
441        	FUNCTION GetEstremiFolder(
442        			CodIdConnectionTokenIn	IN		DMT_CONNECTION_TOKEN.CI_CONNECTION_TOKEN%TYPE,
443        																		--(obblig.) Codice identificativo del token di connessione
444        			IdFolderIn			IN 		DMT_FOLDER.ID_FOLDER%TYPE,		-- (obblig.) Id. del folder 
445        			XMLOut				OUT		NOCOPY CLOB,					-- XML di output conforme allo schema EstremiFolder.xsd
446        																		-- E' valorizzato solo se FlgTipoWSIn=G e in caso di esito positivo
447        			FlgRollBckFullIn	IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se 1 in caso di errore viene fatta la rollback completa (non a savepoint), altrimenti la rollback delle sole modifiche effettuate nella stored
448        																		--ATTENZIONE: La rollback, di qualsiasi tipo, non riguarda la scrittura di eventuali log
449        			FlgAutoCommitIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se vale 1, dopo la rollback e qualunque sia l'esito (positivo o meno), la funzione esegue una commit finale
450        			ErrContextOut		OUT		VARCHAR2,						--Contesto (ovvero package e/o funzione/procedura) in cui si è verificato l'errore; se NULL è la funzione stessa
451        			ErrCodeOut			OUT		PLS_INTEGER,					--N.ro errore di uscita
452        			ErrMsgOut			OUT 		VARCHAR2)					--Messaggio d'errore
453        			RETURN PLS_INTEGER;										--Vale 1 se tutto è andato a buon fine, altrimenti 0
454        
455        	/***************** Funzione per aggiornare i metadati di un folder/libreria (tramite Web Service)		**************/
456        	/***************** i metadati da aggiornare sono forniti in un xml conforme allo schema FolderDaAgg.xsd	**************/
457        	FUNCTION UpdFolder(
458        			CodIdConnectionTokenIn	IN		DMT_CONNECTION_TOKEN.CI_CONNECTION_TOKEN%TYPE,
459        															--(obblig.) Codice identificativo del token di connessione
460        			XMLIn				IN		CLOB,						--(obblig.) XML in input al Web service di aggiornamento folder (secondo schema FolderDaAgg.xsd)
461        			IdFolderOut			OUT		DMT_FOLDER.ID_FOLDER%TYPE,		-- Id. del folder da modificare (ricavato da XMLIn)
462        			XmlOut			OUT		NOCOPY CLOB,				-- XML (secondo schema SezioneCache.xsd) con tutti gli attributi del folder da passare all'API di aggiornamento folder
463        			FlgRollBckFullIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se 1 in caso di errore viene fatta la rollback completa (non a savepoint), altrimenti la rollback delle sole modifiche effettuate nella stored
464        															--ATTENZIONE: La rollback, di qualsiasi tipo, non riguarda la scrittura di eventuali log
465        			FlgAutoCommitIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se vale 1, dopo la rollback e qualunque sia l'esito (positivo o meno), la funzione esegue una commit finale
466        			ErrContextOut		OUT		VARCHAR2,					--Contesto (ovvero package e/o funzione/procedura) in cui si è verificato l'errore; se NULL è la funzione stessa
467        			ErrCodeOut			OUT		PLS_INTEGER,				--N.ro errore di uscita
468        			ErrMsgOut			OUT 		VARCHAR2)					--Messaggio d'errore
469        			RETURN PLS_INTEGER;									--Vale 1 se tutto è andato a buon fine, altrimenti 0
470        
471        
472        	/************ Funzione per cancellare logicamente o fisicamente un folder (anche una libreria) 													**********/
473        	/************ gli estremi del folder da cancellare e del tipo di cancellazione (fisica o logica) sono forniti in un xml conforme a schema DeleteFolder.xsd		  	**********/
474        	FUNCTION DelFolder(
475        			CodIdConnectionTokenIn	IN		DMT_CONNECTION_TOKEN.CI_CONNECTION_TOKEN%TYPE,
476        															--(obblig.) Codice identificativo del token di connessione
477        			XMLIn				IN		CLOB,						--(obblig.) XML in input al Web service di cancellazione versione (secondo schema DeleteFolder.xsd)
478        			IdFolderOut			OUT		DMT_FOLDER.ID_FOLDER%TYPE,		--Identificativo del folder da cancellare
479        			FlgTipoDelOut		OUT		VARCHAR2,					--(valori F/L) Tipo di cancellazione richiesta: F = Fisica, L = Logica
480        			FlgRollBckFullIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se 1 in caso di errore viene fatta la rollback completa (non a savepoint), altrimenti la rollback delle sole modifiche effettuate nella stored
481        															--ATTENZIONE: La rollback, di qualsiasi tipo, non riguarda la scrittura di eventuali log
482        			FlgAutoCommitIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se vale 1, dopo la rollback e qualunque sia l'esito (positivo o meno), la funzione esegue una commit finale
483        			ErrContextOut		OUT		VARCHAR2,					--Contesto (ovvero package e/o funzione/procedura) in cui si è verificato l'errore; se NULL è la funzione stessa
484        			ErrCodeOut			OUT		PLS_INTEGER,				--N.ro errore di uscita
485        			ErrMsgOut			OUT 		VARCHAR2)					--Messaggio d'errore
486        			RETURN PLS_INTEGER;									--Vale 1 se tutto è andato a buon fine, altrimenti 0
487        
488        	/***************** Funzione che a partire da un xml conforme allo schema EstremiXIdentificazioneFolder.xsd ricava il folder su cui lavorare 				******/
489        	/***************** serve per i WS di lock, unlock e GetMetadati. Nel caso del GetMetadati restituisce anche i metadati del folder					 	******/
490        	FUNCTION LockGetDatiFolder(
491        			CodIdConnectionTokenIn	IN		DMT_CONNECTION_TOKEN.CI_CONNECTION_TOKEN%TYPE,
492        															--(obblig.) Codice identificativo del token di connessione
493        			FlgTipoWSIn			IN		VARCHAR2,					--(obblig.) Tipo di Web Service che richiama la funzione. Valori ammessi:
494        															-- L	= Lock
495        															-- U	= Unlock
496        															-- G	= Check-out
497        			XMLIn				IN		CLOB,						--(obblig.) XML con tracciato conforme a schema EstremiXIdentificazioneFolder.xsd
498        			IdFolderOut			OUT		DMT_FOLDER.ID_FOLDER%TYPE,		-- Id. del folder ricavato dall'XML in input
499        			XMLOut			OUT		NOCOPY CLOB,				-- XML di output conforme allo schema Output_DatiFolder.xsd.
500        															-- E' valorizzato solo se FlgTipoWSIn=G e in caso di esito positivo
501        			FlgRollBckFullIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se 1 in caso di errore viene fatta la rollback completa (non a savepoint), altrimenti la rollback delle sole modifiche effettuate nella stored
502        															--ATTENZIONE: La rollback, di qualsiasi tipo, non riguarda la scrittura di eventuali log
503        			FlgAutoCommitIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se vale 1, dopo la rollback e qualunque sia l'esito (positivo o meno), la funzione esegue una commit finale
504        			ErrContextOut		OUT		VARCHAR2,					--Contesto (ovvero package e/o funzione/procedura) in cui si è verificato l'errore; se NULL è la funzione stessa
505        			ErrCodeOut			OUT		PLS_INTEGER,				--N.ro errore di uscita
506        			ErrMsgOut			OUT 		VARCHAR2)					--Messaggio d'errore
507        			RETURN PLS_INTEGER;									--Vale 1 se tutto è andato a buon fine, altrimenti 0
508        
509        
510        	/***************** Funzione per esguire una ricerca sui tipi folder tramite Web Service		**************/
511        	/***************** L'inut deve essere conforme allo schema TrovaTipiFolder.xsd	mentre 		**************/
512        	/***************** l'output è conforme allo schema Output_TrovaTipiFolder.xsd				**************/
513        	FUNCTION TrovaTipiFolder(CodIdConnectionTokenIn		IN	DMT_CONNECTION_TOKEN.CI_CONNECTION_TOKEN%TYPE,	-- (obblig). Codice identificativo del token di connessione
514        							XMLIn						IN	CLOB,						-- XML contenente i dati del tipo di folder (conforme allo schema TrovaTipiFolder.xsd)
515        							XmlOut						OUT	NOCOPY CLOB,				-- XML con dati di output specifici del WS, restituito solo in caso di successo, conforme a schema Output_TrovaTipiFolder.xsd
516        							FlgRollBckFullIn			IN 	PLS_INTEGER DEFAULT NULL,	--(valori 1/0/NULL) Se 1 in caso di errore viene fatta la rollback completa (non a savepoint), altrimenti la rollback delle sole modifiche effettuate nella stored
517        																						--ATTENZIONE: La rollback, di qualsiasi tipo, non riguarda la scrittura di eventuali log
518        							FlgAutoCommitIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se vale 1, dopo la rollback e qualunque sia l'esito (positivo o meno), la funzione esegue una commit finale
519        							ErrContextOut		OUT		VARCHAR2,					--Contesto (ovvero package e/o funzione/procedura) in cui si è verificato l'errore; se NULL è la funzione stessa
520        							ErrCodeOut			OUT		PLS_INTEGER,				--N.ro errore di uscita
521        							ErrMsgOut			OUT 		VARCHAR2)				--Messaggio d'errore
522        							RETURN PLS_INTEGER;
523        
524        
525        	/***************** Funzione per esguire una ricerca sui tipi di documento tramite Web Service		**************/
526        	/***************** L'inut deve essere conforme allo schema TrovaTipiDocumenti.xsd	mentre 			**************/
527        	/***************** l'output è conforme allo schema Output_TrovaTipiDocumenti.xsd					**************/
528        
529        	FUNCTION TrovaTipiDocumento(CodIdConnectionTokenIn		IN	DMT_CONNECTION_TOKEN.CI_CONNECTION_TOKEN%TYPE,	-- (obblig). Codice identificativo del token di connessione
530        							XMLIn						IN	CLOB,						-- XML contenente i dati del tipo di folder (conforme allo schema TrovaTipiDocumenti.xsd)
531        							XmlOut						OUT	NOCOPY CLOB,				-- XML con dati di output specifici del WS, restituito solo in caso di successo, conforme allo schema Output_TrovaTipiDocumenti.xsd
532        							FlgRollBckFullIn			IN 	PLS_INTEGER DEFAULT NULL,	--(valori 1/0/NULL) Se 1 in caso di errore viene fatta la rollback completa (non a savepoint), altrimenti la rollback delle sole modifiche effettuate nella stored
533        																						--ATTENZIONE: La rollback, di qualsiasi tipo, non riguarda la scrittura di eventuali log
534        							FlgAutoCommitIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se vale 1, dopo la rollback e qualunque sia l'esito (positivo o meno), la funzione esegue una commit finale
535        							ErrContextOut		OUT		VARCHAR2,					--Contesto (ovvero package e/o funzione/procedura) in cui si è verificato l'errore; se NULL è la funzione stessa
536        							ErrCodeOut			OUT		PLS_INTEGER,				--N.ro errore di uscita
537        							ErrMsgOut			OUT 		VARCHAR2)				--Messaggio d'errore
538        							RETURN PLS_INTEGER;
539        
540        	/***************** Funzione per creare una nuovo tipo di  documento (tramite Web Service)		**************/
541        	/***************** i cui metadati sono forniti in un xml conforme allo schema NuovoTipoDocumento.xsd	**************/
542        	FUNCTION AddTipoDoc(
543        			CodIdConnectionTokenIn	IN		DMT_CONNECTION_TOKEN.CI_CONNECTION_TOKEN%TYPE,
544        															--(obblig.) Codice identificativo del token di connessione
545        			XMLIn				IN		CLOB,						--(obblig.) XML in input al Web service di creazione tipo documento(secondo schema uovoTipoDocumento.xsd)
546        			FlgRollBckFullIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se 1 in caso di errore viene fatta la rollback completa (non a savepoint), altrimenti la rollback delle sole modifiche effettuate nella stored
547        															--ATTENZIONE: La rollback, di qualsiasi tipo, non riguarda la scrittura di eventuali log
548        			FlgAutoCommitIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se vale 1, dopo la rollback e qualunque sia l'esito (positivo o meno), la funzione esegue una commit finale
549        			IdTipoDocOut		 OUT DMT_DOC_TYPES.ID_DOC_TYPE%TYPE,
550        			ErrContextOut		OUT		VARCHAR2,					--Contesto (ovvero package e/o funzione/procedura) in cui si è verificato l'errore; se NULL è la funzione stessa
551        			ErrCodeOut			OUT		PLS_INTEGER,				--N.ro errore di uscita
552        			ErrMsgOut			OUT 		VARCHAR2)					--Messaggio d'errore
553        			RETURN PLS_INTEGER;									--Vale 1 se tutto è andato a buon fine, altrimenti 0
554        
555        	/***************** Funzione per modificare una nuovo tipo di  documento (tramite Web Service)		**************/
556        	/***************** i cui metadati sono forniti in un xml conforme allo schema ModificaTipoDocumento.xsd	**************/
557        	FUNCTION UpdTipoDoc(
558        			CodIdConnectionTokenIn	IN		DMT_CONNECTION_TOKEN.CI_CONNECTION_TOKEN%TYPE,
559        															--(obblig.) Codice identificativo del token di connessione
560        			XMLIn				IN		CLOB,						--(obblig.) XML in input al Web service di creazione tipo documento(secondo schema uovoTipoDocumento.xsd)
561        			FlgRollBckFullIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se 1 in caso di errore viene fatta la rollback completa (non a savepoint), altrimenti la rollback delle sole modifiche effettuate nella stored
562        															--ATTENZIONE: La rollback, di qualsiasi tipo, non riguarda la scrittura di eventuali log
563        			FlgAutoCommitIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se vale 1, dopo la rollback e qualunque sia l'esito (positivo o meno), la funzione esegue una commit finale
564        			ErrContextOut		OUT		VARCHAR2,					--Contesto (ovvero package e/o funzione/procedura) in cui si è verificato l'errore; se NULL è la funzione stessa
565        			ErrCodeOut			OUT		PLS_INTEGER,				--N.ro errore di uscita
566        			ErrMsgOut			OUT 		VARCHAR2)					--Messaggio d'errore
567        			RETURN PLS_INTEGER;								--Vale 1 se tutto è andato a buon fine, altrimenti 0
568        
569        
570        	/************ Funzione per cancellare logicamente o fisicamente un tipo documento 													**********/
571        	/************ gli estremi del tipo doc da cancellare e del tipo di cancellazione (fisica o logica) sono forniti in un xml conforme a schema DeleteTipoDocumento.xsd		  	**********/
572        	FUNCTION DelTipoDoc(
573        			CodIdConnectionTokenIn	IN		DMT_CONNECTION_TOKEN.CI_CONNECTION_TOKEN%TYPE,
574        															--(obblig.) Codice identificativo del token di connessione
575        			XMLIn				IN		CLOB,						--(obblig.) XML in input al Web service di cancellazione versione (secondo schema DeleteTipoDocumento.xsd)
576        			IdTipoDocOut		OUT DMT_DOC_TYPES.ID_DOC_TYPE%TYPE,		--Identificativo del tipodocumento da cancellare
577        			FlgTipoDelOut		OUT		VARCHAR2,					--(valori F/L) Tipo di cancellazione richiesta: F = Fisica, L = Logica
578        			FlgRollBckFullIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se 1 in caso di errore viene fatta la rollback completa (non a savepoint), altrimenti la rollback delle sole modifiche effettuate nella stored
579        															--ATTENZIONE: La rollback, di qualsiasi tipo, non riguarda la scrittura di eventuali log
580        			FlgAutoCommitIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se vale 1, dopo la rollback e qualunque sia l'esito (positivo o meno), la funzione esegue una commit finale
581        			ErrContextOut		OUT		VARCHAR2,					--Contesto (ovvero package e/o funzione/procedura) in cui si è verificato l'errore; se NULL è la funzione stessa
582        			ErrCodeOut			OUT		PLS_INTEGER,				--N.ro errore di uscita
583        			ErrMsgOut			OUT 		VARCHAR2)					--Messaggio d'errore
584        			RETURN PLS_INTEGER;									--Vale 1 se tutto è andato a buon fine, altrimenti 0
585        
586        
587        
588        
589        	/***************** Funzione per creare una nuovo tipo di  folder/libreria (tramite Web Service)		**************/
590        	/***************** i cui metadati sono forniti in un xml conforme allo schema NuovoTipoFolder.xsd	**************/
591        	FUNCTION AddTipoFolder(
592        			CodIdConnectionTokenIn	IN		DMT_CONNECTION_TOKEN.CI_CONNECTION_TOKEN%TYPE,
593        															--(obblig.) Codice identificativo del token di connessione
594        			XMLIn				IN		CLOB,						--(obblig.) XML in input al Web service di creazione folder (secondo schema NuovoTipoFolder.xsd)
595        				FlgRollBckFullIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se 1 in caso di errore viene fatta la rollback completa (non a savepoint), altrimenti la rollback delle sole modifiche effettuate nella stored
596        															--ATTENZIONE: La rollback, di qualsiasi tipo, non riguarda la scrittura di eventuali log
597        			FlgAutoCommitIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se vale 1, dopo la rollback e qualunque sia l'esito (positivo o meno), la funzione esegue una commit finale
598        			IdTipoFolderOut		 OUT DMT_FOLDER_TYPES.ID_FOLDER_TYPE%TYPE,
599        				ErrContextOut		OUT		VARCHAR2,					--Contesto (ovvero package e/o funzione/procedura) in cui si è verificato l'errore; se NULL è la funzione stessa
600        			ErrCodeOut			OUT		PLS_INTEGER,				--N.ro errore di uscita
601        			ErrMsgOut			OUT 		VARCHAR2)					--Messaggio d'errore
602        			RETURN PLS_INTEGER;									--Vale 1 se tutto è andato a buon fine, altrimenti 0
603        
604        	/***************** Funzione per modificare un tipo di  folder (tramite Web Service)		**************/
605        	/***************** i cui metadati sono forniti in un xml conforme allo schema ModificaTipoFolder.xsd	**************/
606        	FUNCTION UpdTipoFolder(
607        			CodIdConnectionTokenIn	IN		DMT_CONNECTION_TOKEN.CI_CONNECTION_TOKEN%TYPE,
608        															--(obblig.) Codice identificativo del token di connessione
609        			XMLIn				IN		CLOB,						--(obblig.) XML in input al Web service di creazione folder (secondo schema ModificaTipoFolder.xsd)
610        			FlgRollBckFullIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se 1 in caso di errore viene fatta la rollback completa (non a savepoint), altrimenti la rollback delle sole modifiche effettuate nella stored
611        															--ATTENZIONE: La rollback, di qualsiasi tipo, non riguarda la scrittura di eventuali log
612        			FlgAutoCommitIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se vale 1, dopo la rollback e qualunque sia l'esito (positivo o meno), la funzione esegue una commit finale
613        			ErrContextOut		OUT		VARCHAR2,					--Contesto (ovvero package e/o funzione/procedura) in cui si è verificato l'errore; se NULL è la funzione stessa
614        			ErrCodeOut			OUT		PLS_INTEGER,				--N.ro errore di uscita
615        			ErrMsgOut			OUT 		VARCHAR2)					--Messaggio d'errore
616        			RETURN PLS_INTEGER;									--Vale 1 se tutto è andato a buon fine, altrimenti 0
617        
618        
619        	/************ Funzione per cancellare logicamente o fisicamente un tipo folder 													**********/
620        	/************ gli estremi del tipo folder da cancellare e del tipo di cancellazione (fisica o logica) sono forniti in un xml conforme a schema DeleteTipoFolder.xsd		  	**********/
621        	FUNCTION DelTipoFolder(
622        			CodIdConnectionTokenIn	IN		DMT_CONNECTION_TOKEN.CI_CONNECTION_TOKEN%TYPE,
623        															--(obblig.) Codice identificativo del token di connessione
624        			XMLIn				IN		CLOB,						--(obblig.) XML in input al Web service di cancellazione versione (secondo schema DeleteTipoFolder.xsd)
625        			IdTipoFolderOut		 OUT DMT_FOLDER_TYPES.ID_FOLDER_TYPE%TYPE,	--Identificativo del tipo folder da cancellare
626        			FlgTipoDelOut		OUT		VARCHAR2,					--(valori F/L) Tipo di cancellazione richiesta: F = Fisica, L = Logica
627        			FlgRollBckFullIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se 1 in caso di errore viene fatta la rollback completa (non a savepoint), altrimenti la rollback delle sole modifiche effettuate nella stored
628        															--ATTENZIONE: La rollback, di qualsiasi tipo, non riguarda la scrittura di eventuali log
629        			FlgAutoCommitIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se vale 1, dopo la rollback e qualunque sia l'esito (positivo o meno), la funzione esegue una commit finale
630        			ErrContextOut		OUT		VARCHAR2,					--Contesto (ovvero package e/o funzione/procedura) in cui si è verificato l'errore; se NULL è la funzione stessa
631        			ErrCodeOut			OUT		PLS_INTEGER,				--N.ro errore di uscita
632        			ErrMsgOut			OUT 		VARCHAR2)					--Messaggio d'errore
633        			RETURN PLS_INTEGER;									--Vale 1 se tutto è andato a buon fine, altrimenti 0
634        
635        
636        	/***************** Funzione per creare o modificare un attributo custom (tramite Web Service)												**************/
637        	/***************** i cui metadati sono forniti in un xml conforme allo schema NewAttrCustom.xsd	nel caso di creazione, ModifyAttrCustom.xsd nel caso di modifica	**************/
638        	function AddUpdAttrCustom(
639        			CodIdConnectionTokenIn	IN		DMT_CONNECTION_TOKEN.CI_CONNECTION_TOKEN%TYPE,
640        															--(obblig.) Codice identificativo del token di connessione
641        			FlgOperazioneIn		IN		VARCHAR2,					-- (obblig). Indica se si vuole creare (ADD) o modificare (MOD) un attributo custom
642        			XMLIn				IN		CLOB,						--(obblig.) XML in input al Web service di creazione o modifica attributo custom (secondo schema NewAttrCustom.xsd o ModifyAttrCustom.xsd)
643        			FlgRollBckFullIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se 1 in caso di errore viene fatta la rollback completa (non a savepoint), altrimenti la rollback delle sole modifiche effettuate nella stored
644        															--ATTENZIONE: La rollback, di qualsiasi tipo, non riguarda la scrittura di eventuali log
645        			FlgAutoCommitIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se vale 1, dopo la rollback e qualunque sia l'esito (positivo o meno), la funzione esegue una commit finale
646        			ErrContextOut		OUT		VARCHAR2,					--Contesto (ovvero package e/o funzione/procedura) in cui si è verificato l'errore; se NULL è la funzione stessa
647        			ErrCodeOut			OUT		PLS_INTEGER,				--N.ro errore di uscita
648        			ErrMsgOut			OUT 		VARCHAR2)					--Messaggio d'errore
649        			RETURN PLS_INTEGER;									--Vale 1 se tutto è andato a buon fine, altrimenti 0
650        
651        
652        	/************ Funzione per cancellare logicamente o fisicamente un attributo custom																**********/
653        	/************ gli estremi dell'attributo da cancellare e del tipo di cancellazione (fisica o logica) sono forniti in un xml conforme a schema DeleteAttrCustom.xsd		**********/
654        	FUNCTION DelAttrCustom(
655        			CodIdConnectionTokenIn	IN		DMT_CONNECTION_TOKEN.CI_CONNECTION_TOKEN%TYPE,
656        															--(obblig.) Codice identificativo del token di connessione
657        			XMLIn				IN		CLOB,						--(obblig.) XML in input al Web service di cancellazione versione (secondo schema DeleteAttrCustom.xsd)
658        			AttrNameOut			OUT		DMT_ATTRIBUTES_DEF.ATTR_NAME%type,	-- Nome dell'attributo da cancellare
659        			FlgTipoDelOut		OUT		VARCHAR2,					--(valori F/L) Tipo di cancellazione richiesta: F = Fisica, L = Logica
660        			FlgRollBckFullIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se 1 in caso di errore viene fatta la rollback completa (non a savepoint), altrimenti la rollback delle sole modifiche effettuate nella stored
661        															--ATTENZIONE: La rollback, di qualsiasi tipo, non riguarda la scrittura di eventuali log
662        			FlgAutoCommitIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se vale 1, dopo la rollback e qualunque sia l'esito (positivo o meno), la funzione esegue una commit finale
663        			ErrContextOut		OUT		VARCHAR2,					--Contesto (ovvero package e/o funzione/procedura) in cui si è verificato l'errore; se NULL è la funzione stessa
664        			ErrCodeOut			OUT		PLS_INTEGER,				--N.ro errore di uscita
665        			ErrMsgOut			OUT 		VARCHAR2)					--Messaggio d'errore
666        			RETURN PLS_INTEGER;									--Vale 1 se tutto è andato a buon fine, altrimenti 0
667        
668        	/***************** Funzione per creare o modificare un'applicazione esterna o sua specifica istanza (tramite Web Service)	**************/
669        	/***************** i cui metadati sono forniti in un xml conforme allo schema IstanzaApplicazioneEsterna.xsd			**************/
670        	function AddUpdApplicazioneEsterna(
671        			CodIdConnectionTokenIn	IN		DMT_CONNECTION_TOKEN.CI_CONNECTION_TOKEN%TYPE,
672        															--(obblig.) Codice identificativo del token di connessione
673        			XMLIn				IN		CLOB,						--(obblig.) XML in input al Web service di creazione o modifica applicazione esterna /sua istanza(secondo schema IstanzaApplicazioneEsterna.xsd)
674        			FlgRollBckFullIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se 1 in caso di errore viene fatta la rollback completa (non a savepoint), altrimenti la rollback delle sole modifiche effettuate nella stored
675        															--ATTENZIONE: La rollback, di qualsiasi tipo, non riguarda la scrittura di eventuali log
676        			FlgAutoCommitIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se vale 1, dopo la rollback e qualunque sia l'esito (positivo o meno), la funzione esegue una commit finale
677        			ErrContextOut		OUT		VARCHAR2,					--Contesto (ovvero package e/o funzione/procedura) in cui si è verificato l'errore; se NULL è la funzione stessa
678        			ErrCodeOut			OUT		PLS_INTEGER,				--N.ro errore di uscita
679        			ErrMsgOut			OUT 		VARCHAR2)					--Messaggio d'errore
680        			RETURN PLS_INTEGER;									--Vale 1 se tutto è andato a buon fine, altrimenti 0
681        
682        	/************ Funzione per cancellare logicamente o fisicamente un'applicazione esterna o sua specifica istanza (tramite Web Service)													**********/
683        	/************ gli estremi dell'applicazione esterna o sua specifica istanza da cancellare e del tipo di cancellazione (fisica o logica) sono forniti in un xml conforme a schema DeleteIstanzaApplEsterna.xsd	**********/
684        	function DelApplicazioneEsterna(
685        			CodIdConnectionTokenIn	IN		DMT_CONNECTION_TOKEN.CI_CONNECTION_TOKEN%TYPE,
686        															--(obblig.) Codice identificativo del token di connessione
687        			XMLIn				IN		CLOB,						--(obblig.) XML in input al Web service di cancellazione versione (secondo schema DeleteIstanzaApplEsterna.xsd)
688        			FlgTipoDelOut		OUT		VARCHAR2,					--(valori F/L) Tipo di cancellazione richiesta: F = Fisica, L = Logica
689        			FlgRollBckFullIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se 1 in caso di errore viene fatta la rollback completa (non a savepoint), altrimenti la rollback delle sole modifiche effettuate nella stored
690        															--ATTENZIONE: La rollback, di qualsiasi tipo, non riguarda la scrittura di eventuali log
691        			FlgAutoCommitIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se vale 1, dopo la rollback e qualunque sia l'esito (positivo o meno), la funzione esegue una commit finale
692        			ErrContextOut		OUT		VARCHAR2,					--Contesto (ovvero package e/o funzione/procedura) in cui si è verificato l'errore; se NULL è la funzione stessa
693        			ErrCodeOut			OUT		PLS_INTEGER,				--N.ro errore di uscita
694        			ErrMsgOut			OUT 		VARCHAR2)					--Messaggio d'errore
695        			RETURN PLS_INTEGER;									--Vale 1 se tutto è andato a buon fine, altrimenti 0
696        
697        	/***************** Funzione per estrarre i metadati di un'applicazione esterna o sua specifica istanza (tramite Web Service)					************/
698        	/************ gli estremi dell'applicazione esterna o sua specifica istanza sono forniti in un xml conforme allo schema EstremiIstanzaApplEsterna.xsd	************/
699        	FUNCTION GetDatiApplicazioneEsterna(
700        			CodIdConnectionTokenIn	IN		DMT_CONNECTION_TOKEN.CI_CONNECTION_TOKEN%TYPE,
701        															--(obblig.) Codice identificativo del token di connessione
702        			XMLIn				IN		CLOB,						--(obblig.) XML in input al Web service di estrazione metadati di un'applicazione esterna/sua istanza (secondo schema EstremiIstanzaApplEsterna.xsd)
703        			XmlOut			OUT		NOCOPY CLOB,				-- XML con dati di output specifici del WS, restituito solo in caso di successo, conforme a schema Output_IstanzaApplEsterna.xsd
704        			FlgRollBckFullIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se 1 in caso di errore viene fatta la rollback completa (non a savepoint), altrimenti la rollback delle sole modifiche effettuate nella stored
705        															--ATTENZIONE: La rollback, di qualsiasi tipo, non riguarda la scrittura di eventuali log
706        			FlgAutoCommitIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se vale 1, dopo la rollback e qualunque sia l'esito (positivo o meno), la funzione esegue una commit finale
707        			ErrContextOut		OUT		VARCHAR2,					--Contesto (ovvero package e/o funzione/procedura) in cui si è verificato l'errore; se NULL è la funzione stessa
708        			ErrCodeOut			OUT		PLS_INTEGER,				--N.ro errore di uscita
709        			ErrMsgOut			OUT 		VARCHAR2)					--Messaggio d'errore
710        			RETURN PLS_INTEGER;									--Vale 1 se tutto è andato a buon fine, altrimenti 0
711        
712        	/***************** Funzione per esguire una ricerca sulle applicazioni esterne e loro istanze tramite Web Service		**************/
713        	/***************** L'inut deve essere conforme allo schema TrovaIstanzeApplEsterne.xsd mentre 					**************/
714        	/***************** l'output è conforme allo schema Output_TrovaIstanzeApplEsterne.xsd						**************/
715        	FUNCTION TrovaApplicazioneEsterne(
716        			CodIdConnectionTokenIn	IN	DMT_CONNECTION_TOKEN.CI_CONNECTION_TOKEN%TYPE,	
717        															-- (obblig). Codice identificativo del token di connessione
718        			XMLIn				IN	CLOB,							-- XML di input del WS (conforme allo schema TrovaIstanzeApplEsterne.xsd) contenente i filtri di ricerca e la modalità di estrazione
719        			XmlOut			OUT	NOCOPY CLOB,					-- XML con dati di output specifici del WS, restituito solo in caso di successo, conforme a schema Output_TrovaIstanzeApplEsterne.xsd
720        			FlgRollBckFullIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se 1 in caso di errore viene fatta la rollback completa (non a savepoint), altrimenti la rollback delle sole modifiche effettuate nella stored
721        															--ATTENZIONE: La rollback, di qualsiasi tipo, non riguarda la scrittura di eventuali log
722        			FlgAutoCommitIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se vale 1, dopo la rollback e qualunque sia l'esito (positivo o meno), la funzione esegue una commit finale
723        			ErrContextOut		OUT		VARCHAR2,					--Contesto (ovvero package e/o funzione/procedura) in cui si è verificato l'errore; se NULL è la funzione stessa
724        			ErrCodeOut			OUT		PLS_INTEGER,				--N.ro errore di uscita
725        			ErrMsgOut			OUT 		VARCHAR2)					--Messaggio d'errore
726        			RETURN PLS_INTEGER;									--Vale 1 se tutto è andato a buon fine, altrimenti 0
727        
728        
729        
730        	/***************** Funzione per settare i privilegi su un oggetto tramite Web Service		**************/
731        	/***************** L'input deve essere conforme allo schema IUPrivilegi.xsd  				**************/
732        
733        	FUNCTION AddUpdPrivilegi(
734        			CodIdConnectionTokenIn	IN	DMT_CONNECTION_TOKEN.CI_CONNECTION_TOKEN%TYPE,	
735        															-- (obblig). Codice identificativo del token di connessione
736        			XMLIn				IN	CLOB,							-- XML di input del WS (conforme allo schema IUPrivilegi.xsd)  
737        			FlgRollBckFullIn	IN 		PLS_INTEGER DEFAULT NULL,	--(valori 1/0/NULL) Se 1 in caso di errore viene fatta la rollback completa (non a savepoint), altrimenti la rollback delle sole modifiche effettuate nella stored
738        																	--ATTENZIONE: La rollback, di qualsiasi tipo, non riguarda la scrittura di eventuali log
739        			FlgAutoCommitIn		IN 		PLS_INTEGER DEFAULT NULL,	--(valori 1/0/NULL) Se vale 1, dopo la rollback e qualunque sia l'esito (positivo o meno), la funzione esegue una commit finale
740        			ErrContextOut		OUT		VARCHAR2,					--Contesto (ovvero package e/o funzione/procedura) in cui si è verificato l'errore; se NULL è la funzione stessa
741        			ErrCodeOut			OUT		PLS_INTEGER,				--N.ro errore di uscita
742        			ErrMsgOut			OUT 		VARCHAR2)				--Messaggio d'errore
743        			RETURN PLS_INTEGER;									--Vale 1 se tutto è andato a buon fine, altrimenti 0
744        
745        	/***************** Funzione per revocare i privilegi su un oggetto tramite Web Service		**************/
746        	/***************** L'input deve essere conforme allo schema RPrivilegi.xsd mentre 			**************/
747        	FUNCTION RevokePrivilegi(
748        			CodIdConnectionTokenIn	IN	DMT_CONNECTION_TOKEN.CI_CONNECTION_TOKEN%TYPE,	
749        																	-- (obblig). Codice identificativo del token di connessione
750        			XMLIn				IN	CLOB,							-- XML di input del WS (conforme allo schema RPrivilegi.xsd)  
751        			FlgRollBckFullIn	IN 		PLS_INTEGER DEFAULT NULL,	--(valori 1/0/NULL) Se 1 in caso di errore viene fatta la rollback completa (non a savepoint), altrimenti la rollback delle sole modifiche effettuate nella stored
752        																	--ATTENZIONE: La rollback, di qualsiasi tipo, non riguarda la scrittura di eventuali log
753        			FlgAutoCommitIn		IN 		PLS_INTEGER DEFAULT NULL,	--(valori 1/0/NULL) Se vale 1, dopo la rollback e qualunque sia l'esito (positivo o meno), la funzione esegue una commit finale
754        			ErrContextOut		OUT		VARCHAR2,					--Contesto (ovvero package e/o funzione/procedura) in cui si è verificato l'errore; se NULL è la funzione stessa
755        			ErrCodeOut			OUT		PLS_INTEGER,				--N.ro errore di uscita
756        			ErrMsgOut			OUT 		VARCHAR2)				--Messaggio d'errore
757        			RETURN PLS_INTEGER ;	
758        
759        	/***************** Funzione per creare un nuovo utente								**************/
760        	/***************** L'input deve essere conforme allo schema NuovoUser.xsd			**************/
761        	FUNCTION AddUser(
762        			CodIdConnectionTokenIn	IN	DMT_CONNECTION_TOKEN.CI_CONNECTION_TOKEN%TYPE,	
763        															-- (obblig). Codice identificativo del token di connessione
764        			XMLIn				IN	CLOB,							-- XML di input del WS (conforme allo schema IUPrivilegi.xsd)  
765        			FlgRollBckFullIn	IN 		PLS_INTEGER DEFAULT NULL,	--(valori 1/0/NULL) Se 1 in caso di errore viene fatta la rollback completa (non a savepoint), altrimenti la rollback delle sole modifiche effettuate nella stored
766        																	--ATTENZIONE: La rollback, di qualsiasi tipo, non riguarda la scrittura di eventuali log
767        			FlgAutoCommitIn		IN 		PLS_INTEGER DEFAULT NULL,	--(valori 1/0/NULL) Se vale 1, dopo la rollback e qualunque sia l'esito (positivo o meno), la funzione esegue una commit finale
768        			ErrContextOut		OUT		VARCHAR2,					--Contesto (ovvero package e/o funzione/procedura) in cui si è verificato l'errore; se NULL è la funzione stessa
769        			ErrCodeOut			OUT		PLS_INTEGER,				--N.ro errore di uscita
770        			ErrMsgOut			OUT 		VARCHAR2)				--Messaggio d'errore
771        			RETURN PLS_INTEGER;									--Vale 1 se tutto è andato a buon fine, altrimenti 0
772        
773        	/***************** Funzione per modificare un utente								**************/
774        	/***************** L'input deve essere conforme allo schema ModificaUser.xsd			**************/
775        	FUNCTION ModUser(
776        			CodIdConnectionTokenIn	IN	DMT_CONNECTION_TOKEN.CI_CONNECTION_TOKEN%TYPE,	
777        															-- (obblig). Codice identificativo del token di connessione
778        			XMLIn				IN	CLOB,							-- XML di input del WS (conforme allo schema IUPrivilegi.xsd)  
779        			FlgRollBckFullIn	IN 		PLS_INTEGER DEFAULT NULL,	--(valori 1/0/NULL) Se 1 in caso di errore viene fatta la rollback completa (non a savepoint), altrimenti la rollback delle sole modifiche effettuate nella stored
780        																	--ATTENZIONE: La rollback, di qualsiasi tipo, non riguarda la scrittura di eventuali log
781        			FlgAutoCommitIn		IN 		PLS_INTEGER DEFAULT NULL,	--(valori 1/0/NULL) Se vale 1, dopo la rollback e qualunque sia l'esito (positivo o meno), la funzione esegue una commit finale
782        			ErrContextOut		OUT		VARCHAR2,					--Contesto (ovvero package e/o funzione/procedura) in cui si è verificato l'errore; se NULL è la funzione stessa
783        			ErrCodeOut			OUT		PLS_INTEGER,				--N.ro errore di uscita
784        			ErrMsgOut			OUT 		VARCHAR2)				--Messaggio d'errore
785        			RETURN PLS_INTEGER;									--Vale 1 se tutto è andato a buon fine, altrimenti 0
786        
787        	/***************** Funzione per cancellare un utente								**************/
788        	/***************** L'input deve essere conforme allo schema EliminaUser.xsd			**************/
789        	FUNCTION DelUser(
790        			CodIdConnectionTokenIn	IN	DMT_CONNECTION_TOKEN.CI_CONNECTION_TOKEN%TYPE,	
791        															-- (obblig). Codice identificativo del token di connessione
792        			XMLIn				IN	CLOB,							-- XML di input del WS (conforme allo schema IUPrivilegi.xsd)  
793        			FlgRollBckFullIn	IN 		PLS_INTEGER DEFAULT NULL,	--(valori 1/0/NULL) Se 1 in caso di errore viene fatta la rollback completa (non a savepoint), altrimenti la rollback delle sole modifiche effettuate nella stored
794        																	--ATTENZIONE: La rollback, di qualsiasi tipo, non riguarda la scrittura di eventuali log
795        			FlgAutoCommitIn		IN 		PLS_INTEGER DEFAULT NULL,	--(valori 1/0/NULL) Se vale 1, dopo la rollback e qualunque sia l'esito (positivo o meno), la funzione esegue una commit finale
796        			ErrContextOut		OUT		VARCHAR2,					--Contesto (ovvero package e/o funzione/procedura) in cui si è verificato l'errore; se NULL è la funzione stessa
797        			ErrCodeOut			OUT		PLS_INTEGER,				--N.ro errore di uscita
798        			ErrMsgOut			OUT 		VARCHAR2)				--Messaggio d'errore
799        			RETURN PLS_INTEGER;									--Vale 1 se tutto è andato a buon fine, altrimenti 0
800        
801        	/************* Funzione attraverso cui registrarsi come utente della community di AURIGA **************/
802        	function RegistrazioneUtenteCommunity(
803        			XMLIn				IN		CLOB,						-- XML di input del WS (conforme allo schema RegistrazioneNellaCommunity.xsd.xsd) contenente gli estremi dell'utente che si sta registrando
804        			FlgRollBckFullIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se 1 in caso di errore viene fatta la rollback completa (non a savepoint), altrimenti la rollback delle sole modifiche effettuate nella stored
805        															--ATTENZIONE: La rollback, di qualsiasi tipo, non riguarda la scrittura di eventuali log
806        			FlgAutoCommitIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se vale 1, dopo la rollback e qualunque sia l'esito (positivo o meno), la funzione esegue una commit finale
807        			ErrContextOut		OUT		VARCHAR2,					--Contesto (ovvero package e/o funzione/procedura) in cui si è verificato l'errore; se NULL è la funzione stessa
808        			ErrCodeOut			OUT		PLS_INTEGER,				--N.ro errore di uscita
809        			ErrMsgOut			OUT 		VARCHAR2)					--Messaggio d'errore
810        			RETURN PLS_INTEGER;									--Vale 1 se tutto è andato a buon fine, altrimenti 0
811        
812        	/************* Funzione utilizzata dal WS che serve ad avviare un nuovo thread di discussione in un forum **************/
813        	function NewForumThread(
814        			CodIdConnectionTokenIn	IN		DMT_CONNECTION_TOKEN.CI_CONNECTION_TOKEN%TYPE,
815        															--(obblig.) Codice identificativo del token di connessione
816        			XMLIn				IN		CLOB,						-- XML di input del WS (conforme allo schema NewForumThread.xsd.xsd) contenente i dati di avvio del thread
817        			AttrFolderXmlOut		OUT		NOCOPY CLOB,				-- XML (conforme a schema SezioneCache.xsd) da passare in input all’API di creazione folder che viene invocata per prima all'interno del WS per creare la cartella del thread
818        			AttrDocPrimarioXMLOut	OUT		NOCOPY CLOB,				-- XML (conforme a schema SezioneCache.xsd) da passare in input all’API di creazione documento che viene invocata per seconda all'interno del WS per creare l’unità documentaria che conterrà i dati di avvio del thread
819        			DocAttachXMLOut		OUT		NOCOPY CLOB,				-- Lista con i dati per caricare (con step-version) nella repository i file associati all'unità documentaria di avvio thread che sono specificati come attach del messaggio SOAP(XML conforme a schema LISTA_STD.xsd)
820        															-- Per ogni attach vi è un tag Riga che contiene le seguenti colonne:
821        															-- 1: Indica il tipo di operazione da fare in corrispondenza dell'attach: AD = Add doc + creazione versione (per i file secondari); AV = Solo creazione versione (per il file primario)
822        															-- 2: N.ro dell'attach del messaggio SOAP che corrisponde al file da caricare nella repository
823        															-- 3: Nome del file da caricare (quello con cui mostrarlo e dalla cui estensione si ricava il formato)
824        															-- 4: (valori 1/0) Se 1 indica che è richiesta la verifica della firma digitale del file, se 0 o NULL no
825        			FlgRollBckFullIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se 1 in caso di errore viene fatta la rollback completa (non a savepoint), altrimenti la rollback delle sole modifiche effettuate nella stored
826        															--ATTENZIONE: La rollback, di qualsiasi tipo, non riguarda la scrittura di eventuali log
827        			FlgAutoCommitIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se vale 1, dopo la rollback e qualunque sia l'esito (positivo o meno), la funzione esegue una commit finale
828        			ErrContextOut		OUT		VARCHAR2,					--Contesto (ovvero package e/o funzione/procedura) in cui si è verificato l'errore; se NULL è la funzione stessa
829        			ErrCodeOut			OUT		PLS_INTEGER,				--N.ro errore di uscita
830        			ErrMsgOut			OUT 		VARCHAR2)					--Messaggio d'errore
831        			RETURN PLS_INTEGER;									--Vale 1 se tutto è andato a buon fine, altrimenti 0
832        
833        
834        	/************* Funzione per ottenere l'XML di output (primo attach del messaggio SOAP) del WS che serve ad avviare un nuovo thread di discussione in un forum *********/
835        	/************* (a partire dell'id. dell'unità documentaria di avvio del thread)				 									  *********/
836        	function ComponiOutput_ForumThread(
837        			IdUDAvvioThreadIn		IN		DMT_UNITA_DOC.ID_UD%type,		-- (obblig.) Id. dell'unità documentaria di avvio del thread
838        			XMLOut			OUT 		NOCOPY CLOB,				-- XML che viene restituito come primo attach del WS in caso di successo (conforme allo schema Output_ForumThread.xsd)
839        			ErrContextOut		OUT		VARCHAR2,					-- Contesto (ovvero package e/o funzione/procedura) in cui si è verificato l'errore; se NULL è la funzione stessa
840        			ErrCodeOut			OUT		PLS_INTEGER,				-- N.ro errore di uscita
841        			ErrMsgOut			OUT 		VARCHAR2)					-- Messaggio d'errore
842        			RETURN PLS_INTEGER;									-- Vale 1 se tutto è andato a buon fine, altrimenti 0
843        
844        	/***** funzione utilizzata dai web services SendRepositoryDocsByFax o SendByFax per leggere l'XML in input *********/ 
845        	function SendByFax(
846        			CodIdConnectionTokenIn	IN		DMT_CONNECTION_TOKEN.CI_CONNECTION_TOKEN%TYPE,
847        															-- (obblig. solo nel caso del WS di SendRepositoryDocsByFax) Codice identificativo del token di connessione
848        			XMLIn				IN		CLOB,						-- XML di input del WS (conforme allo schema SendRepositoryDocsByFax.xsd o SendByFax.xsd)
849        			NroFaxDestinatarioOut	OUT		VARCHAR2,					-- N.ro fax destinatario
850        			FaxServerAddressOut	OUT		VARCHAR2,					-- IP o nome del fax-server mittente
851        			UsernameFaxServerOut	OUT		VARCHAR2,					-- Username con cui accedere al fax-server mittente
852        			PasswordFaxServerOut	OUT		VARCHAR2,					-- Password con cui accedere al fax-server mittente
853        			DocToExtractOut		OUT		NOCOPY CLOB,				-- Lista (secondo schema LISTA_STD.xsd) con gli id. dei documenti di cui inviare l'ultima versione (valorizzata solo nel caso del WS di SendRepositoryDocsByFax)
854        															-- Ogni tag Riga contiene le colonne:
855        															-- 1: ID_DOC del documento
856        															-- 2: Estensione che identifica il formato del file (doc, pdf, tiff, ecc) di ultima versione
857        			ErrContextOut		OUT		VARCHAR2,					-- Contesto (ovvero package e/o funzione/procedura) in cui si è verificato l'errore; se NULL è la funzione stessa
858        			ErrCodeOut			OUT		PLS_INTEGER,				-- N.ro errore di uscita
859        			ErrMsgOut			OUT 		VARCHAR2)					-- Messaggio d'errore
860        			RETURN PLS_INTEGER;									-- Vale 1 se tutto è andato a buon fine, altrimenti 0
861        
862        
863        	/***************** Funzione per esguire tramite Web Service una ricerca sulle richieste di trasmissione via fax già sottomesse  	**************/
864        	/***************** L'input deve essere conforme allo schema TrovaRichTrasmissioneFax.xsd	 							**************/
865        	/***************** l'output è conforme allo schema Output_TrovaRichTrasmissioneFax.xsd								**************/
866        	function TrovaRichTrasmissioneFax(
867        			CodIdConnectionTokenIn	IN	DMT_CONNECTION_TOKEN.CI_CONNECTION_TOKEN%TYPE,	
868        															-- (obblig). Codice identificativo del token di connessione
869        			XMLIn				IN	CLOB,							-- XML contenente i filtri di ricerca(conforme allo schema TrovaRichTrasmissioneFax.xsd)
870        			XmlOut			OUT	NOCOPY CLOB,					-- XML con dati di output specifici del WS, restituito solo in caso di successo, conforme a schema Output_TrovaRichTrasmissioneFax.xsd
871        			FlgRollBckFullIn		IN 	PLS_INTEGER DEFAULT NULL,			--(valori 1/0/NULL) Se 1 in caso di errore viene fatta la rollback completa (non a savepoint), altrimenti la rollback delle sole modifiche effettuate nella stored
872        															-- ATTENZIONE: La rollback, di qualsiasi tipo, non riguarda la scrittura di eventuali log
873        			FlgAutoCommitIn		IN 	PLS_INTEGER DEFAULT NULL,			--(valori 1/0/NULL) Se vale 1, dopo la rollback e qualunque sia l'esito (positivo o meno), la funzione esegue una commit finale
874        			ErrContextOut		OUT	VARCHAR2,						-- Contesto (ovvero package e/o funzione/procedura) in cui si è verificato l'errore; se NULL è la funzione stessa
875        			ErrCodeOut			OUT	PLS_INTEGER,					-- N.ro errore di uscita
876        			ErrMsgOut			OUT 	VARCHAR2)						-- Messaggio d'errore
877        			RETURN PLS_INTEGER;									-- Vale 1 se tutto è andato a buon fine, altrimenti 0
878        
879        	/***************** Funzione per per il Web Service di ricerca sulle repository documentale			**************/
880        	/***************** L'input deve essere conforme allo schema TrovaDocFolder.xsd	 				**************/
881        	/***************** l'output sono gli input da passare all'API di 	ricerca sulle repository documentale	**************/
882        	function TrovaDocFolder(
883        			CodIdConnectionTokenIn	IN	DMT_CONNECTION_TOKEN.CI_CONNECTION_TOKEN%TYPE,	
884        															-- (obblig). Codice identificativo del token di connessione
885        			XMLIn				IN	CLOB,							-- XML in input al WS (conforme allo schema TrovaDocFolder.xsd)
886        			FlgUDFolderOut		OUT	VARCHAR2,						-- (valori F/U/NULL): indica se la ricerca va ristretta ai soli documenti (U) o alle sole cartelle (F)
887        			CercaInFolderOut		OUT	DMT_FOLDER.ID_FOLDER%type,			-- id. del folder in cui effettuare la ricerca. Se non specificato si cerca in tutta la repository
888        															--	Può avere i valori "particolari":
889        															--		-9:		si intende la ricerca nelle NEWS dell'utente di lavoro 
890        															--		-99:		si intende la ricerca nell'area WORK dell'utente di lavoro
891        															--		-999: 	si intende la ricerca nei preferiiti dell'utente di lavoro (solo i preferiti veri e propri, non ciò che ricade in cartelle che sono nei preferiti)
892        															--		-9999:	si intende la ricerca negli inviati dell'utente di lavoro (solo gli inviati veri e propri, non ciò che ricade in cartelle che sono negli inviati)
893        															--		-99999:	si intende la ricerca negli eliminati dell'utente di lavoro (solo gli eliminati veri e propri, non ciò che ricade in cartelle che sono negli eliminati)
894        			FlgCercaInSubFolderOut	OUT	PLS_INTEGER,					-- (valori 1/0/NULL): 1 indica che la ricerca va estesa ai sub-folder del folder CercaInFolderOut 
895        			FiltroFullTextOut		OUT	VARCHAR2,						-- Parola o lista di parole da cercare con il motore di ricerca esterno (Lucene)
896        			FlgTutteLeParoleOut	OUT	PLS_INTEGER,					-- (valori 1/0/NULL) Se 1 tutte le parole del filtro full-text devono essere matchate, se 0 o NULL almeno 1
897        			AttributiXRicercaFTOut	OUT	NOCOPY CLOB,					-- XML contenente la lista degli attributi indicizzati dal motore di ricerca su quali deve ricercare il filtro full-text (conforme a schema LISTA_STD.xsd)
898        															-- Ogni attributo corrisponde ad un tag riga con una sola colonna recante il nome dell'attributo stesso
899        			CriteriAvanzatiOut	OUT	NOCOPY CLOB,					-- XML contenente nomi e valori dei filtri avanzati su unità documentarie e folder (conforme allo schema SezioneCache.xsd). 
900        			CriteriPersonalizzatiOut OUT 	NOCOPY CLOB,					-- Criteri di ricerca sugli attributi custom di documenti e folder (XML conforme a schema LISTA_STD.xsd). Se più di uno vengono applicati in AND.
901        			ColOrderByOut		OUT	VARCHAR2,						-- Indica il/i numeri della/e colonna dell'XML di output per cui ordinare i record nell'XML stesso 
902        			FlgDescOrderByOut  	OUT	VARCHAR2,						-- E' una stringa di 1 o 0/NULL, tanti quanti le colonne di ordinamento, separati da ",": 1 significa che l'ordinamento per la data colonna deve essere discendente anzichè ascendente (es: 1; 1,0; 1, )
903        			FlgSenzaPaginazioneOut	OUT	PLS_INTEGER,            			-- (valori 1/0/NULL) Se 1 l'estrazione non è paginata, altrimenti sì
904        			NroPaginaOut      	OUT	PLS_INTEGER,                  		-- E' il numero della pagina da estrarre; se NULL e l'estrazione è paginata viene estratta la prima pagina
905           			BachSizeOut			OUT	PLS_INTEGER,					-- Indica la dimensione (n.ro di record) della pagina se l'estrazione è paginata e se non è valorizzato la dimensione della pagina è quella settata per l'utente di lavoro o è pari al valore del parametro STD_PAGE_NUM_ROW del dominio di lavoro				
906        			FlgBatchSearchOut		OUT	PLS_INTEGER,					-- (valori 1/0/NULL) Se 1 significa che la ricerca è utilizzata da un processo batch, altrimenti da un processo online.
907        															-- Serve affinchè vengano applicati i limiti di overflow per processo batch o online. 
908        			ColToReturnOut		OUT	VARCHAR2,						-- Stringa con i n.ri delle colonne dell'XML di output che si vuole siano presenti nell'output stesso.
909        			FlgRollBckFullIn		IN 	PLS_INTEGER DEFAULT NULL,			--(valori 1/0/NULL) Se 1 in caso di errore viene fatta la rollback completa (non a savepoint), altrimenti la rollback delle sole modifiche effettuate nella stored
910        															--ATTENZIONE: La rollback, di qualsiasi tipo, non riguarda la scrittura di eventuali log
911        			FlgAutoCommitIn		IN 	PLS_INTEGER DEFAULT NULL,			--(valori 1/0/NULL) Se vale 1, dopo la rollback e qualunque sia l'esito (positivo o meno), la funzione esegue una commit finale
912        			ErrContextOut		OUT	VARCHAR2,						--Contesto (ovvero package e/o funzione/procedura) in cui si è verificato l'errore; se NULL è la funzione stessa
913        			ErrCodeOut			OUT	PLS_INTEGER,					--N.ro errore di uscita
914        			ErrMsgOut			OUT 	VARCHAR2)						--Messaggio d'errore
915        			RETURN PLS_INTEGER;									-- Vale 1 se tutto è andato a buon fine, altrimenti 0
916        
917        	/***************** Funzione che a partire da un xml conforme allo schema EstremiXIdentificazioneDoc.xsd ricava le versioni di un documento visibili all'utente connesso		******/
918        	FUNCTION GetListaVersioniDoc(
919        			CodIdConnectionTokenIn	IN		DMT_CONNECTION_TOKEN.CI_CONNECTION_TOKEN%TYPE,
920        															--(obblig.) Codice identificativo del token di connessione
921        			XMLIn				IN		CLOB,						--(obblig.) XML con tracciato conforme a schema EstremiXIdentificazioneDoc.xsd
922        			IdDocOut			OUT		DMT_DOCUMENTS.ID_DOC%TYPE,		-- Id. del documento ricavato dall'XML in input
923        			XMLOut			OUT		NOCOPY CLOB,				-- XML di output conforme allo schema ListaVersioniDoc.xsd.
924        															-- E' valorizzato solo in caso di esito positivo
925        			FlgRollBckFullIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se 1 in caso di errore viene fatta la rollback completa (non a savepoint), altrimenti la rollback delle sole modifiche effettuate nella stored
926        															--ATTENZIONE: La rollback, di qualsiasi tipo, non riguarda la scrittura di eventuali log
927        			FlgAutoCommitIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se vale 1, dopo la rollback e qualunque sia l'esito (positivo o meno), la funzione esegue una commit finale
928        			ErrContextOut		OUT		VARCHAR2,					--Contesto (ovvero package e/o funzione/procedura) in cui si è verificato l'errore; se NULL è la funzione stessa
929        			ErrCodeOut			OUT		PLS_INTEGER,				--N.ro errore di uscita
930        			ErrMsgOut			OUT 		VARCHAR2)					--Messaggio d'errore
931        			RETURN PLS_INTEGER;									--Vale 1 se tutto è andato a buon fine, altrimenti 0
932        
933        	/***************** Funzione per esguire una ricerca sugli utenti del sistema tramite Web Service		**************/
934        	/***************** L'inut deve essere conforme allo schema TrovaUtenti.xsd mentre 					**************/
935        	/***************** l'output è conforme allo schema Output_TrovaUtenti.xsd						**************/
936        	FUNCTION TrovaUtenti(
937        			CodIdConnectionTokenIn	IN		DMT_CONNECTION_TOKEN.CI_CONNECTION_TOKEN%TYPE,	
938        															-- (obblig). Codice identificativo del token di connessione
939        			XMLIn				IN		CLOB,						-- XML di input del WS (conforme allo schema TrovaUtenti.xsd) contenente i filtri di ricerca e la modalità di estrazione
940        			XmlOut			OUT		NOCOPY CLOB,				-- XML con dati di output specifici del WS, restituito solo in caso di successo, conforme a schema Output_TrovaUtenti.xsd
941        			FlgRollBckFullIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se 1 in caso di errore viene fatta la rollback completa (non a savepoint), altrimenti la rollback delle sole modifiche effettuate nella stored
942        															--ATTENZIONE: La rollback, di qualsiasi tipo, non riguarda la scrittura di eventuali log
943        			FlgAutoCommitIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se vale 1, dopo la rollback e qualunque sia l'esito (positivo o meno), la funzione esegue una commit finale
944        			ErrContextOut		OUT		VARCHAR2,					--Contesto (ovvero package e/o funzione/procedura) in cui si è verificato l'errore; se NULL è la funzione stessa
945        			ErrCodeOut			OUT		PLS_INTEGER,				--N.ro errore di uscita
946        			ErrMsgOut			OUT 		VARCHAR2)					--Messaggio d'errore
947        			RETURN PLS_INTEGER;									--Vale 1 se tutto è andato a buon fine, altrimenti 0
948        
949        
950        	/***************** Funzione per esguire una ricerca sui gruppi (di utenti, UO, scrivanie virtuali, altri gruppi) tramite Web Service	**************/
951        	/***************** L'inut deve essere conforme allo schema TrovaGruppi.xsd mentre 										**************/
952        	/***************** l'output è conforme allo schema Output_TrovaGruppi.xsd											**************/
953        	FUNCTION TrovaGruppi(
954        			CodIdConnectionTokenIn	IN		DMT_CONNECTION_TOKEN.CI_CONNECTION_TOKEN%TYPE,	
955        															-- (obblig). Codice identificativo del token di connessione
956        			XMLIn				IN		CLOB,						-- XML di input del WS (conforme allo schema TrovaGruppi.xsd) contenente i filtri di ricerca e la modalità di estrazione
957        			XmlOut			OUT		NOCOPY CLOB,				-- XML con dati di output specifici del WS, restituito solo in caso di successo, conforme a schema Output_TrovaGruppi.xsd
958        			FlgRollBckFullIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se 1 in caso di errore viene fatta la rollback completa (non a savepoint), altrimenti la rollback delle sole modifiche effettuate nella stored
959        															--ATTENZIONE: La rollback, di qualsiasi tipo, non riguarda la scrittura di eventuali log
960        			FlgAutoCommitIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se vale 1, dopo la rollback e qualunque sia l'esito (positivo o meno), la funzione esegue una commit finale
961        			ErrContextOut		OUT		VARCHAR2,					--Contesto (ovvero package e/o funzione/procedura) in cui si è verificato l'errore; se NULL è la funzione stessa
962        			ErrCodeOut			OUT		PLS_INTEGER,				--N.ro errore di uscita
963        			ErrMsgOut			OUT 		VARCHAR2)					--Messaggio d'errore
964        			RETURN PLS_INTEGER;									--Vale 1 se tutto è andato a buon fine, altrimenti 0
965        
966        
967        	/***************** Funzione utilizzata dal WS di notifica unità documentaria o folder per leggere l'XML di input ed estrarne i dati da passare all'API di notifica	**************/
968        	/***************** L'inut deve essere conforme allo schema NotificaDocFolder.xsd  															**************/
969        	FUNCTION NotificaDocFolder(
970        			CodIdConnectionTokenIn	IN		DMT_CONNECTION_TOKEN.CI_CONNECTION_TOKEN%TYPE,	
971        															-- (obblig). Codice identificativo del token di connessione
972        			XMLIn				IN		CLOB,						-- XML di input del WS (conforme allo schema NotificaDocFolder.xsd) contenente estreemi dell'UD o folder da notificare, destinatari della notifica e altri attributi della notifica
973        			FlgTypeObjToNotifOut	OUT		VARCHAR2,					-- (valori F/U) Tipo di oggetto da notificare/condividere: F = Folder, U = Unità documentaria 
974        			IdObjToNotifOut		OUT		INTEGER,					-- Identificativo (ID_FOLDER o ID_UD) del folder o unità documentaria da notificare/condividere
975        			RecipientsXMLOut		OUT		NOCOPY CLOB,				-- Lista dei destinatari (xml conforme a schema LISTA_STD.xsd)
976        															-- Ogni destinatario è un tag "Riga" con le seguenti colonne:
977        															--						1: (obblig.) Indicatore del tipo di destinastario
978        															--							Valori possibili:
979        															-- 							UT 	= Utente
980        															--							SV	= Scrivania virtuale
981        															--							UO	= Unità organizzativa (vale a dire tutte le scrivanie virtuali ad essa afferenti)
982        															--							G	= Gruppo
983        															--							R	= Ruolo amministrativo contestualizzato ovvero i soggetti che hanno un certo ruolo amministrativo (eventualmente rispetto ad una certa UO o un dato livello della struttura organizzativa/tipo di UO o entrambi)
984        															--							R@	= Soggetti interni che hanno un certo ruolo rispetto al documento / folder
985        															--						2: Identificativo del destinatario
986        															--							è un ID_UO di DMT_STRUTTURA_ORG se colonna 1=UO,
987        															--							è un ID_GRUPPO di DMT_GRUPPI se colonna 1 =G
988        															--							è un ID_USER di DMT_USERS se colonna 1 =UT
989        															--							è un ID_SCRIVANIA di DMT_SCRIVANIE_VIRTUALI se colonna 1 =SV
990        															--							è un ID_RUOLO_AMM di DMT_RUOLI_AMM se colonna 1 =R
991        															--							è un COD_NATURA_REL di DMT_REL_SOGG_INT_DOC/DMT_REL_SOGG_INT_FOLDER che indica il codice del ruolo rispetto al documento / folder se colonna 1 = R@. Può avere i valori particolari: #CON_ACCESSO (= tutti coloro che hanno accesso anche in sola visualizzazione) e #CON_ACCESSO_IN_MODIFICA (= tutti coloro che hanno accesso anche in modifica dei metadati e/o file)
992        															--						3: Denominazione/nome del destinatario (se trattasi di UO può essere la denominazione semplice o preceduta da quella di tutte le UO superiori separate da "|"). Se colonna 1 = R@ è la descrizione del ruolo rispetto al documento / folder
993        															--						4: Identificativo della UO cui appartiene la scrivania destinataria
994        															--						5: Nri livello della UO destinataria o a cui appartiene la scrivania destinataria (tutti o alcuni; però se si specifica un livello è obbligatorio indicare tutti i livelli superiori; vanno specificati come appaiono nella GUI, ovvero romani se previsto, 0 o -- se nulli; sono separati dal separatore dei livelli valido per il soggetto produttore/AOO di appartenenza della UO)
995        															--						6: Denominazione della UO cui appartiene la scrivania destinataria (può essere la denominazione semplice o preceduta da quella di tutte le UO superiori separate da "|")
996        															--						7: Codice che identifica nell'applicazione esterna da cui ci si connette l'utente destinatario o quello che occupa la scrivania destinataria
997        															--						8: Denominazione dell'utente che occupa la scrivania destinataria
998        															--						9: (valore 1) Indica che il destinatario, se UO, sono anche tutte le sue sotto-UO
999        															--						10: (valori interi da 1 a n) Livello della struttura organizzativa in cui si espleta il ruolo amministrativo destinatario
1000        															--						11: Se colonna 1 =R: Codice del tipo di unità organizzativa in cui si espleta il ruolo amministrativo destinatario (alternativo alla colonna 10)
1001        															--						    Se colonna 1 =UT o SV: Username (delle credenziali locali) dell'utente destinatario o che occupa la scrivania destinataria 
1002        															--						12: Se colonna 1 =R: Descrizione del tipo di unità organizzativa (es: settore, servizio, ecc) in cui si espleta il ruolo amministrativo destinatario (alternativo alla colonna 10)
1003        															--						    Se colonna 1 =UT o SV: N.ro di matricola dell'utente destinatario o che occupa la scrivania destinataria 
1004        															--						13: Identificativo della UO in cui si espleta il ruolo amministrativo destinatario o dalla quale risalire alla/e UO del livello/tipo indicati in cui si espleta il ruolo amministrativo destinatario
1005        															--						14: Denominazione/nome della UO in cui si espleta il ruolo amministrativo destinatario o dalla quale risalire alla/e UO del livello/tipo indicati in cui si espleta il ruolo amministrativo destinatario (può essere la denominazione semplice o preceduta da quella di tutte le UO superiori separate da "|")
1006        															--						15: Nri livello della UO in cui si espleta il ruolo amministrativo destinatario o dalla quale risalire alla/e UO del livello/tipo indicati in cui si espleta il ruolo amministrativo destinatario (tutti o alcuni; però se si specifica un livello è obbligatorio indicare tutti i livelli superiori; vanno specificati come appaiono nella GUI, ovvero romani se previsto, 0 o -- se nulli; sono separati dal separatore dei livelli valido per il soggetto produttore/AOO di appartenenza della UO)
1007        															--						16:(valore 1) Indica che il ruolo amministrativo destinatario si espleta anche nelle sotto-UO della/e UO rintracciate tramite i valori delle 3 colonne precedenti
1008        															--						17: Codice del ruolo del destinatario (valori codificati in apposita disctionary entry)
1009        															--						18: Ruolo del destinatario (valori in apposita disctionary entry)
1010        															--						19: Flag che indica la modalità di accesso da dare al destinatario sull'oggetto inviato (ovvero indica le opzioni di accesso che avrà il destinatario nell'ACL dell'oggetto dopo l'invio, salvo ne avesse già di più ampie)
1011        															--						    Valori possibili: FC = Full control; M = Modifica metadati e files/contenuti; MM = Modifica soli metadati; V = Sola visualizzazione; VM = Visualizzazione dei soli metadati (non dei files)
1012        			SenderTypeOut		OUT		VARCHAR2,					-- (valori UO/UT/SV) Indica il tipo di soggetto mittente della notifica
1013        															-- (gestito solo per unità documentaria) Valori ammessi: UO = Unità organizzativa; UT = Utente; SV = Scrivania
1014        			SenderIdOut			OUT		INTEGER,					-- (gestito solo per unità documentaria) Identificativo (ID_UO o ID_USER o ID_SCRIVANIA) del soggetto mittente della notifica	
1015        			CodMotivoNotifOut		OUT		VARCHAR2,					-- Codice del motivo della notifica (valori codificati in apposita dictionary entry)
1016        			MotivoNotifOut		OUT		VARCHAR2,					-- Modivo della notifica (valori da apposita dictionary entry)
1017        			MessaggioNotifOut		OUT		VARCHAR2,					-- Messaggio della notifica
1018        			LivelloPrioritaOut	OUT		INTEGER,					-- Livello di priorità della notifica 
1019        			RichConfermaPresaVisOut OUT		PLS_INTEGER,				-- (valori 1/0) Se 1 significa che si richiede di avere una notifica alla prima visione dell'unità doc./folder da parte di un destinatario
1020        			FlgEmailNotifPresaVisOut	OUT		PLS_INTEGER,			-- (valori 1/0) Se 1 e se RichConfermaPresaVisOut=1 indica che la notifica di avvenuta presa visione deve essere data anche via email
1021        			IndEmailNotifPresaVisOut	OUT		VARCHAR2,				-- Indirizzi email (se più d'uno separati da ; o ,) a cui inviare la mail di notifica di presa visione 
1022        															-- Se non è valorizzato e RichConfermaPresaVisOut=1 e FlgEmailNotifPresaVisOut=1 allora la notifica email di presa visione viene data all'indirizzo email del mittente della notifica originale
1023        			NotNoPresaVisEntroGGOut	OUT		PLS_INTEGER,				-- N.ro di giorni trascorsi i quali dalla decorrenza della notifica senza che il documento/folder notificato sia stato visionato dal destinatario deve esserene dato avvertimento al mittente/a qualcuno
1024        			FlgEmailNoPresaVisOut	OUT		PLS_INTEGER,				-- (valori 1/0) Se 1 e se l'input NotNoPresaVisEntroGGOut è valorizzato indica che la notifica di mancata presa visione entro il termine stabilito deve essere data anche via email
1025        			IndEmailNoPresaVisOut	OUT		VARCHAR2,					-- Indirizzi email (se più d'uno separati da ; o ,) a cui inviare la mail di notifica di mancata presa visione entro il termine NotNoPresaVisEntroGGOut.
1026        															-- Se non è valorizzato e NotNoPresaVisEntroGGOut è valorizzato e FlgEmailNoPresaVisOut=1 allora la notifica email di mancata presa visione viene data all'indirizzo email del mittente della notifica originale
1027        			TsDecorrenzaNotifOut	OUT		VARCHAR2,					-- Data e ora future da cui deve decorrere la notifica (se diversa dalla data di notifica; espressa nel formato dato dal parametro di config. FMT_STD_TIMESTAMP)
1028        			FlgNotificaEmailNotifOut OUT		PLS_INTEGER,				-- (valori 1/0) Se 1 significa che ci deve essere notifica email ai destinatari (o a degli indirizzi email indicati nell'argomento IndXNotifEmailNotifIn)
1029        			IndXNotifEmailNotifOut	OUT		VARCHAR2,					-- Indirizzi email (se più d'uno separati da ; o ,) a cui notificare la notifica 
1030        			OggEmailOut			OUT		VARCHAR2,					-- (valorizzato se IndXNotifEmailNotifOut è valorizzato) Oggetto dell'email di notifica
1031        			BodyEmailOut		OUT		VARCHAR2,					-- (valorizzato se IndXNotifEmailNotifOut è valorizzato) Corpo del messaggio dell'email di notifica
1032        			FlgNotificaSMSNotifOut	OUT		PLS_INTEGER,				-- (valori 1/0) Se 1 significa che ci deve essere notifica via SMS ai destinatari (o a n.ri di cellulare indicati nell'argomento NriCellXNotifSMSNotifOut)
1033        			NriCellXNotifSMSNotifOut OUT		VARCHAR2,					-- N.ri di cellulare (se più d'uno separati da ; o ,) a cui notificare via SMS
1034        			TestoSMSOut			OUT		VARCHAR2,					-- (valorizzato se NriCellXNotifSMSNotifOut è valorizzato) Testo dell'SMS di notifica
1035        			FlgRollBckFullIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se 1 in caso di errore viene fatta la rollback completa (non a savepoint), altrimenti la rollback delle sole modifiche effettuate nella stored
1036        															--ATTENZIONE: La rollback, di qualsiasi tipo, non riguarda la scrittura di eventuali log
1037        			FlgAutoCommitIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se vale 1, dopo la rollback e qualunque sia l'esito (positivo o meno), la funzione esegue una commit finale
1038        			ErrContextOut		OUT		VARCHAR2,					--Contesto (ovvero package e/o funzione/procedura) in cui si è verificato l'errore; se NULL è la funzione stessa
1039        			ErrCodeOut			OUT		PLS_INTEGER,				--N.ro errore di uscita
1040        			ErrMsgOut			OUT 		VARCHAR2)					--Messaggio d'errore
1041        			RETURN PLS_INTEGER;									--Vale 1 se tutto è andato a buon fine, altrimenti 0
1042        
1043        
1044        	/********** funzione utilizzata dai Web Service per aggiungere/togliere un unità documentaria o un folder dai preferiti dell'utente connesso *******/
1045        	function AddRemoveFromFavourite(
1046        			CodIdConnectionTokenIn	IN		DMT_CONNECTION_TOKEN.CI_CONNECTION_TOKEN%TYPE,
1047        															--(obblig.) Codice identificativo del token di connessione
1048        			OperationTypeIn		IN		VARCHAR2 DEFAULT 'ADD',			--(obblig.) Indica se l'operazione da fare è:
1049        															-- ADD = Aggiunta ai preferiti dell'utente
1050        															-- REMOVE = Eliminazione dai preferiti dell'utente
1051        			FlgTypeObjIn		IN		VARCHAR2,					-- (obblig.) Indica se l'oogetto da aggiungere o togliere dai preferiti è:
1052        															-- F = Folder
1053        															-- U = Unità documentaria
1054        			XMLIn				IN		CLOB,						--(obblig.) XML in input al Web service (secondo schema EstremiXIdentificazioneUD.xsd o EstemiXIdentificazioneFolder.xsd)
1055        			FlgRollBckFullIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se 1 in caso di errore viene fatta la rollback completa (non a savepoint), altrimenti la rollback delle sole modifiche effettuate nella stored
1056        															--ATTENZIONE: La rollback, di qualsiasi tipo, non riguarda la scrittura di eventuali log
1057        			FlgAutoCommitIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se vale 1, dopo la rollback e qualunque sia l'esito (positivo o meno), la funzione esegue una commit finale
1058        			ErrContextOut		OUT		VARCHAR2,					--Contesto (ovvero package e/o funzione/procedura) in cui si è verificato l'errore; se NULL è la funzione stessa
1059        			ErrCodeOut			OUT		PLS_INTEGER,				--N.ro errore di uscita
1060        			ErrMsgOut			OUT 		VARCHAR2)					--Messaggio d'errore
1061        			RETURN PLS_INTEGER;									--Vale 1 se tutto è andato a buon fine, altrimenti 0
1062        
1063        	/***************** Funzione che a partire da un xml conforme allo schema EstremiXIdentificazioneUd.xsd ricava quali sono le "versioni" dei metadati dell'unità documentaria	******/
1064        	FUNCTION GetListaVersioniDatiUD(
1065        			CodIdConnectionTokenIn	IN		DMT_CONNECTION_TOKEN.CI_CONNECTION_TOKEN%TYPE,
1066        															--(obblig.) Codice identificativo del token di connessione
1067        			XMLIn				IN		CLOB,						--(obblig.) XML con tracciato conforme a schema EstremiXIdentificazioneUd.xsd
1068        			XMLOut			OUT		NOCOPY CLOB,				-- XML di output conforme allo schema ListaVersioniDatiUD.xsd.
1069        															-- E' valorizzato solo in caso di esito positivo
1070        			FlgRollBckFullIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se 1 in caso di errore viene fatta la rollback completa (non a savepoint), altrimenti la rollback delle sole modifiche effettuate nella stored
1071        															--ATTENZIONE: La rollback, di qualsiasi tipo, non riguarda la scrittura di eventuali log
1072        			FlgAutoCommitIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se vale 1, dopo la rollback e qualunque sia l'esito (positivo o meno), la funzione esegue una commit finale
1073        			ErrContextOut		OUT		VARCHAR2,					--Contesto (ovvero package e/o funzione/procedura) in cui si è verificato l'errore; se NULL è la funzione stessa
1074        			ErrCodeOut			OUT		PLS_INTEGER,				--N.ro errore di uscita
1075        			ErrMsgOut			OUT 		VARCHAR2)					--Messaggio d'errore
1076        			RETURN PLS_INTEGER;									--Vale 1 se tutto è andato a buon fine, altrimenti 0
1077        
1078        	/***************** Funzione che a partire da un xml conforme allo schema GetListaNotificheUD.xsd ricava quali sono le notifiche (salvo quelle automatiche) sull'unità documentaria	******/
1079        	FUNCTION GetListaNotificheUD(
1080        			CodIdConnectionTokenIn	IN		DMT_CONNECTION_TOKEN.CI_CONNECTION_TOKEN%TYPE,
1081        															--(obblig.) Codice identificativo del token di connessione
1082        			XMLIn				IN		CLOB,						--(obblig.) XML con tracciato conforme a schema GetListaNotificheUD.xsd
1083        			XMLOut			OUT		NOCOPY CLOB,				-- XML di output conforme allo schema ListaNotificheUD.xsd
1084        															-- E' valorizzato solo in caso di esito positivo
1085        			FlgRollBckFullIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se 1 in caso di errore viene fatta la rollback completa (non a savepoint), altrimenti la rollback delle sole modifiche effettuate nella stored
1086        															--ATTENZIONE: La rollback, di qualsiasi tipo, non riguarda la scrittura di eventuali log
1087        			FlgAutoCommitIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se vale 1, dopo la rollback e qualunque sia l'esito (positivo o meno), la funzione esegue una commit finale
1088        			ErrContextOut		OUT		VARCHAR2,					--Contesto (ovvero package e/o funzione/procedura) in cui si è verificato l'errore; se NULL è la funzione stessa
1089        			ErrCodeOut			OUT		PLS_INTEGER,				--N.ro errore di uscita
1090        			ErrMsgOut			OUT 		VARCHAR2)					--Messaggio d'errore
1091        			RETURN PLS_INTEGER;									--Vale 1 se tutto è andato a buon fine, altrimenti 0
1092        
1093        	/***************** Funzione che a partire da un xml conforme allo schema EstremiXIdentificazioneUd.xsd ricava quali sono i soggetti interni a cui si possono mandare osservazioni/commenti sull'unità documentaria	******/
1094        	FUNCTION GetPossibiliDestOsservazSuUD(
1095        			CodIdConnectionTokenIn	IN		DMT_CONNECTION_TOKEN.CI_CONNECTION_TOKEN%TYPE,
1096        															--(obblig.) Codice identificativo del token di connessione
1097        			XMLIn				IN		CLOB,						--(obblig.) XML con tracciato conforme a schema EstremiXIdentificazioneUd.xsd
1098        			XMLOut			OUT		NOCOPY CLOB,				-- XML di output conforme allo schema PossbiliDestinatariOsservazioniSuUD.xsd
1099        															-- E' valorizzato solo in caso di esito positivo
1100        			FlgRollBckFullIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se 1 in caso di errore viene fatta la rollback completa (non a savepoint), altrimenti la rollback delle sole modifiche effettuate nella stored
1101        															--ATTENZIONE: La rollback, di qualsiasi tipo, non riguarda la scrittura di eventuali log
1102        			FlgAutoCommitIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se vale 1, dopo la rollback e qualunque sia l'esito (positivo o meno), la funzione esegue una commit finale
1103        			ErrContextOut		OUT		VARCHAR2,					--Contesto (ovvero package e/o funzione/procedura) in cui si è verificato l'errore; se NULL è la funzione stessa
1104        			ErrCodeOut			OUT		PLS_INTEGER,				--N.ro errore di uscita
1105        			ErrMsgOut			OUT 		VARCHAR2)					--Messaggio d'errore
1106        			RETURN PLS_INTEGER;									--Vale 1 se tutto è andato a buon fine, altrimenti 0
1107        
1108        
1109        	/***************** Funzione che a partire da un xml conforme allo schema ExtractFilesInFolder.xsd sottomette nella tabella BMT_JOBS un job di estrazione file di folder ******/
1110        	function ExtractFilesInFolder(
1111        			CodIdConnectionTokenIn	IN		DMT_CONNECTION_TOKEN.CI_CONNECTION_TOKEN%TYPE,
1112        															--(obblig.) Codice identificativo del token di connessione
1113        			XMLIn				IN		CLOB,						--(obblig.) XML con tracciato conforme a schema ExtractFilesInFolder.xsd
1114        			IdJobOut			OUT		INTEGER,					-- Id. del job creato per evadere la richiesta di estrazione file di un folder
1115        			FlgRollBckFullIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se 1 in caso di errore viene fatta la rollback completa (non a savepoint), altrimenti la rollback delle sole modifiche effettuate nella stored
1116        															--ATTENZIONE: La rollback, di qualsiasi tipo, non riguarda la scrittura di eventuali log
1117        			FlgAutoCommitIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se vale 1, dopo la rollback e qualunque sia l'esito (positivo o meno), la funzione esegue una commit finale
1118        			ErrContextOut		OUT		VARCHAR2,					--Contesto (ovvero package e/o funzione/procedura) in cui si è verificato l'errore; se NULL è la funzione stessa
1119        			ErrCodeOut			OUT		PLS_INTEGER,				--N.ro errore di uscita
1120        			ErrMsgOut			OUT 		VARCHAR2)					--Messaggio d'errore
1121        			RETURN PLS_INTEGER;									--Vale 1 se tutto è andato a buon fine, altrimenti 0
1122        
1123        
1124        	/***************** Funzione per esguire una ricerca su strutture e utenti in organigramma		**************/
1125        	/***************** L'inut deve essere conforme allo schema TrovaInOrganigramma.xsd mentre 		**************/
1126        	/***************** l'output è conforme allo schema Output_TrovaInOrganigramma.xsd				**************/
1127        	FUNCTION TrovaInOrganigramma(
1128        			CodIdConnectionTokenIn	IN		DMT_CONNECTION_TOKEN.CI_CONNECTION_TOKEN%TYPE,	
1129        																			-- (obblig). Codice identificativo del token di connessione
1130        			XMLIn				IN		CLOB,								-- XML di input del WS (conforme allo schema TrovaInOrganigramma.xsd) contenente i filtri di ricerca e la modalità di estrazione
1131        			XmlOut				OUT		NOCOPY CLOB,						-- XML con dati di output specifici del WS, restituito solo in caso di successo, conforme a schema Output_TrovaInOrganigramma.xsd
1132        			ErrContextOut		OUT		VARCHAR2,							-- Contesto (ovvero package e/o funzione/procedura) in cui si è verificato l'errore; se NULL è la funzione stessa
1133        			ErrCodeOut			OUT		PLS_INTEGER,						-- N.ro errore di uscita
1134        			ErrMsgOut			OUT 	VARCHAR2)							-- Messaggio d'errore
1135        			RETURN PLS_INTEGER;												-- Vale 1 se tutto è andato a buon fine, altrimenti 0
1136        
1137        	/***************** Funzione per esguire una ricerca di classificazioni in un piano di classificazione (=titolario)		**************/
1138        	/***************** L'inut deve essere conforme allo schema TrovaClassifTitolario.xsd mentre 							**************/
1139        	/***************** l'output è conforme allo schema Output_TrovaClassifTitolario.xsd										**************/
1140        	FUNCTION TrovaClassifTitolario(
1141        			CodIdConnectionTokenIn	IN		DMT_CONNECTION_TOKEN.CI_CONNECTION_TOKEN%TYPE,	
1142        																			-- (obblig). Codice identificativo del token di connessione
1143        			XMLIn				IN		CLOB,								-- XML di input del WS (conforme allo schema TrovaClassifTitolario.xsd) contenente i filtri di ricerca e la modalità di estrazione
1144        			XmlOut				OUT		NOCOPY CLOB,						-- XML con dati di output specifici del WS, restituito solo in caso di successo, conforme a schema Output_TrovaClassifTitolario.xsd
1145        			ErrContextOut		OUT		VARCHAR2,							-- Contesto (ovvero package e/o funzione/procedura) in cui si è verificato l'errore; se NULL è la funzione stessa
1146        			ErrCodeOut			OUT		PLS_INTEGER,						-- N.ro errore di uscita
1147        			ErrMsgOut			OUT 	VARCHAR2)							-- Messaggio d'errore
1148        			RETURN PLS_INTEGER;												-- Vale 1 se tutto è andato a buon fine, altrimenti 0
1149        
1150        	/***************** Funzione per esguire una ricerca di documenti per SUA di ADSPMLO											**************/
1151        	/***************** L'inut deve essere conforme allo schema RicercaDocXImportInBOGestPratiche.xsd mentre 					**************/
1152        	/***************** l'output è conforme allo schema Output_RicercaDocXImportInBOGestPratiche.xsd								**************/
1153        
1154        	FUNCTION TrovaDocPraticaSUA_ADSP(
1155        			CodIdConnectionTokenIn	IN		DMT_CONNECTION_TOKEN.CI_CONNECTION_TOKEN%TYPE,	
1156        																			-- (obblig). Codice identificativo del token di connessione
1157        			XMLIn				IN		CLOB,								-- XML di input del WS (conforme allo schema RicercaDocXImportInBOGestPratiche.xsd) contenente i filtri di ricerca e la modalità di estrazione
1158        			XmlOut				OUT		NOCOPY CLOB,						-- XML con dati di output specifici del WS, restituito solo in caso di successo, conforme a schema Output_RicercaDocXImportInBOGestPratiche.xsd
1159        			ErrContextOut		OUT		VARCHAR2,							-- Contesto (ovvero package e/o funzione/procedura) in cui si è verificato l'errore; se NULL è la funzione stessa
1160        			ErrCodeOut			OUT		PLS_INTEGER,						-- N.ro errore di uscita
1161        			ErrMsgOut			OUT 	VARCHAR2)							-- Messaggio d'errore
1162        			RETURN PLS_INTEGER;												-- Vale 1 se tutto è andato a buon fine, altrimenti 0
1163        
1164        
1165        	/***************** Funzione per ottenere le pubblicazioni all'Albo di una data tipopologia									**************/
1166        	/***************** L'inut deve essere conforme allo schema RequestElencaPubblicazioni.xsd mentre 							**************/
1167        	/***************** l'output è conforme allo schema ResponseElencaPubblicazioni.xsd											**************/
1168        	FUNCTION ElencaPubblicazioni(
1169        			CodIdConnectionTokenIn	IN		DMT_CONNECTION_TOKEN.CI_CONNECTION_TOKEN%TYPE,	
1170        																			-- (obblig). Codice identificativo del token di connessione
1171        			XMLIn				IN		CLOB,								-- XML di input del WS (conforme allo schema RequestElencaPubblicazioni.xsd) contenente i filtri di ricerca e la modalità di estrazione
1172        			XmlOut				OUT		NOCOPY CLOB,						-- XML con dati di output specifici del WS, restituito solo in caso di successo, conforme a schema ResponseElencaPubblicazioni.xsd
1173        			ErrContextOut		OUT		VARCHAR2,							-- Contesto (ovvero package e/o funzione/procedura) in cui si è verificato l'errore; se NULL è la funzione stessa
1174        			ErrCodeOut			OUT		PLS_INTEGER,						-- N.ro errore di uscita
1175        			ErrMsgOut			OUT 	VARCHAR2)							-- Messaggio d'errore
1176        			RETURN PLS_INTEGER;												-- Vale 1 se tutto è andato a buon fine, altrimenti 0
1177        
1178        	/***************** Funzione per esguire una ricerca sui registri di numerazione tramite Web Service			**************/
1179        	/***************** L'inut deve essere conforme allo schema TrovaRegistriNumerazione.xsd mentre 				**************/
1180        	/***************** l'output è conforme allo schema Output_TrovaRegistriNumerazione.xsd						**************/
1181        	FUNCTION TrovaRegistriNumerazione(
1182        			CodIdConnectionTokenIn	IN	DMT_CONNECTION_TOKEN.CI_CONNECTION_TOKEN%TYPE,	
1183        																			-- (obblig). Codice identificativo del token di connessione
1184        			XMLIn					IN	CLOB,								-- XML di input del WS (conforme allo schema TrovaRegistriNumerazione.xsd) contenente i filtri di ricerca e la modalità di estrazione
1185        			XmlOut					OUT	NOCOPY CLOB,						-- XML con dati di output specifici del WS, restituito solo in caso di successo, conforme a schema Output_TrovaRegistriNumerazione.xsd
1186        			FlgRollBckFullIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se 1 in caso di errore viene fatta la rollback completa (non a savepoint), altrimenti la rollback delle sole modifiche effettuate nella stored
1187        																			--ATTENZIONE: La rollback, di qualsiasi tipo, non riguarda la scrittura di eventuali log
1188        			FlgAutoCommitIn		IN 		PLS_INTEGER DEFAULT NULL,			--(valori 1/0/NULL) Se vale 1, dopo la rollback e qualunque sia l'esito (positivo o meno), la funzione esegue una commit finale
1189        			ErrContextOut		OUT		VARCHAR2,							--Contesto (ovvero package e/o funzione/procedura) in cui si è verificato l'errore; se NULL è la funzione stessa
1190        			ErrCodeOut			OUT		PLS_INTEGER,						--N.ro errore di uscita
1191        			ErrMsgOut			OUT 		VARCHAR2)						--Messaggio d'errore
1192        			RETURN PLS_INTEGER;												--Vale 1 se tutto è andato a buon fine, altrimenti 0
1193        
1194        
1195        	/***************** Funzione per ricercare documenti contabili e fascicolarli nella pratica eventuale indicata				**************/
1196        	/***************** L'inut deve essere conforme allo schema Request_GetDocumentiContabili.xsd mentre 					    **************/
1197        	/***************** l'output è conforme allo schema Response_GetDocumentiContabili.xsd										**************/
1198        	FUNCTION RicercaDocContabili(
1199        			CodIdConnectionTokenIn	IN		DMT_CONNECTION_TOKEN.CI_CONNECTION_TOKEN%TYPE,	
1200        																			-- (obblig). Codice identificativo del token di connessione
1201        			XMLIn				IN		CLOB,								-- XML di input del WS (conforme allo schema Request_GetDocumentiContabili) contenente gli estremi dei documenti contabili da ricercare
1202        			XmlOut				OUT		NOCOPY CLOB,						-- XML con dati di output specifici del WS, restituito solo in caso di successo, conforme a schema Response_GetDocumentiContabili.xsd
1203        			ErrContextOut		OUT		VARCHAR2,							-- Contesto (ovvero package e/o funzione/procedura) in cui si è verificato l'errore; se NULL è la funzione stessa
1204        			ErrCodeOut			OUT		PLS_INTEGER,						-- N.ro errore di uscita
1205        			ErrMsgOut			OUT 	VARCHAR2)							-- Messaggio d'errore
1206        			RETURN PLS_INTEGER;												-- Vale 1 se tutto è andato a buon fine, altrimenti 0
1207        
1208        	/***************** Funzione per esguire una ricerca di atti corrispondenti ad un dato progetto di opere pubbliche	**************/
1209        	/***************** L'inut deve essere conforme allo schema RequestRicercaAttiPerProgettoLLPP.xsd mentre 			**************/
1210        	/***************** l'output è conforme allo schema ResponsetRicercaAttiPerProgettoLLPP.xsd							**************/
1211        	FUNCTION RicercaAttiPerProgettoLLPP(
1212        			CodIdConnectionTokenIn	IN		DMT_CONNECTION_TOKEN.CI_CONNECTION_TOKEN%TYPE,	
1213        																			-- (obblig). Codice identificativo del token di connessione
1214        			XMLIn				IN		CLOB,								-- XML di input del WS (conforme allo schema RequestRicercaAttiPerProgettoLLPP.xsd)
1215        			XmlOut				OUT		NOCOPY CLOB,						-- XML di response SEMPRE restituito, con dati di output specifici del WS, conforme a schema ResponsetRicercaAttiPerProgettoLLPP.xsd
1216        			ErrContextOut		OUT		VARCHAR2,							-- Contesto (ovvero package e/o funzione/procedura) in cui si è verificato l'errore; se NULL è la funzione stessa
1217        			ErrCodeOut			OUT		PLS_INTEGER,						-- N.ro errore di uscita
1218        			ErrMsgOut			OUT 	VARCHAR2)							-- Messaggio d'errore
1219        			RETURN PLS_INTEGER;												-- Restituise sempre 1
1220        
1221        	/***************** Funzione per recuperare i dati di un atto necessari ai sistemi di acquisti				**************/
1222        	/***************** L'inut deve essere conforme allo schema RequestGetAttoPerAcquisti.xsd mentre 			**************/
1223        	/***************** l'output è conforme allo schema ResponseGetAttoPerAcquisti.xsd							**************/
1224        	FUNCTION GetAttoPerAcquisti(
1225        			CodIdConnectionTokenIn	IN		DMT_CONNECTION_TOKEN.CI_CONNECTION_TOKEN%TYPE,	
1226        																			-- (obblig). Codice identificativo del token di connessione
1227        			XMLIn				IN		CLOB,								-- XML di input del WS (conforme allo schema RequestGetAttoPerAcquisti.xsd)
1228        			XmlOut				OUT		NOCOPY CLOB,						-- XML di response SEMPRE restituito, con dati di output specifici del WS, conforme a schema ResponseGetAttoPerAcquisti.xsd
1229        			ErrContextOut		OUT		VARCHAR2,							-- Contesto (ovvero package e/o funzione/procedura) in cui si è verificato l'errore; se NULL è la funzione stessa
1230        			ErrCodeOut			OUT		PLS_INTEGER,						-- N.ro errore di uscita
1231        			ErrMsgOut			OUT 	VARCHAR2)							-- Messaggio d'errore
1232        			RETURN PLS_INTEGER;												-- Restituise sempre 1
1233        
1234        	/***************** Funzione per recuperare i dati di pubblicazione di un atto								**************/
1235        	/***************** L'inut deve essere conforme allo schema RequestGetDatiPubblicazioneAtto.xsd mentre 		**************/
1236        	/***************** l'output è conforme allo schema ResponseGetDatiPubblicazioneAtto.xsd						**************/
1237        	FUNCTION GetDatiPubblicazioneAtto(
1238        			CodIdConnectionTokenIn	IN		DMT_CONNECTION_TOKEN.CI_CONNECTION_TOKEN%TYPE,	
1239        																			-- (obblig). Codice identificativo del token di connessione
1240        			XMLIn				IN		CLOB,								-- XML di input del WS (conforme allo schema RequestGetDatiPubblicazioneAtto.xsd)
1241        			XmlOut				OUT		NOCOPY CLOB,						-- XML di response SEMPRE restituito, con dati di output specifici del WS, conforme a schema ResponseGetDatiPubblicazioneAtto.xsd
1242        			URIRelatePubblOut	OUT		NOCOPY CLOB,						-- Lista xml (conforme a schema ListaStd.xsd) con UTI della/e relate di pubblicazione.
1243        																			-- Ogni relata è una riga contenente una sola colonna:
1244        																			-- 1) URI della relata in notazione StorageUtil
1245        			ErrContextOut		OUT		VARCHAR2,							-- Contesto (ovvero package e/o funzione/procedura) in cui si è verificato l'errore; se NULL è la funzione stessa
1246        			ErrCodeOut			OUT		PLS_INTEGER,						-- N.ro errore di uscita
1247        			ErrMsgOut			OUT 	VARCHAR2)							-- Messaggio d'errore
1248        			RETURN PLS_INTEGER;												-- Restituise sempre 1
1249        
1250        	/****************funzione di ricerca agibilità inviata da portale ***************/
1251        	/****************se va a buon fine effettua una protocollazione in uscita *******/
1252        	function RicercaAgibilitaDaPortale(
1253        			XMLRequestIn			IN		CLOB,							-- (obblig.) Tag XML ricercaAgibilitaRequest con i dati della request
1254        			ConnTokenOut			OUT		VARCHAR2,						-- Token di connessione da usare per caricare i file di richiesta e risposta	
1255        			IdDominioOut			OUT     INTEGER,						-- Id. del dominio in cui viene evasa la richiesta	
1256        			IdUDRichiestaOut		OUT		INTEGER,						-- ID_UD assegnato alla richiesta protocollata
1257        			IdDocRichiestaOut		OUT		INTEGER,						-- ID_DOC del documento primario della richiesta protocollata
1258        			EstremiProRichOut		OUT		VARCHAR2,						-- Estremi di protocollo in entrata assegnato alla richiesta
1259        			ListaAgibilitaOut 		OUT		NOCOPY CLOB,					-- XML con la lista dei certificati di agibilità restituiti (ogni riga ha una sola colonna)
1260        			ListaFileOut			OUT		NOCOPY CLOB,					-- XML con la lista dei file da restituire. Ogni file-riga contiene le colonne
1261        																				-- 1) URI in notazione storageUtil
1262        																				-- 2) Nome con cui mostrare il file
1263        																				-- 3) mimetype
1264        																				-- 4) dimensione in bytes
1265        			ModalitaFileXPortaleOut	OUT     VARCHAR2,						-- Modalità di restituzione dei file agibiltà su portale; valori possibili:
1266        																			-- PREVIEW_PDF
1267        																			-- DOWNLOAD_ZIP
1268        			IdUDRispostaOut			OUT		INTEGER,						-- ID_UD assegnato alla risposta protocollata	
1269        			IdDocRispostaOut		OUT		INTEGER,						-- ID_DOC del documento primario della risposta protocollata	
1270        			EstremiProRispostaOut	OUT		VARCHAR2,						-- Estremi di protocollo in uscita assegnato alla risposta
1271        			IdUtenteInvioMailOut	OUT		VARCHAR2,						-- Id. dell'utente del modulo mail da usare per la trasmissione mail della risposta
1272        			AccountMittMailToSendOut OUT	VARCHAR2,						-- Account e-mail da cui inviare la mail con la risposta
1273        			SubjectMailToSendOut	OUT		VARCHAR2,						-- Oggetto della mail da inviare
1274        			BodyMailToSendOut		OUT		NOCOPY CLOB,					-- Corpo della mail da inviare (html)
1275        			FileAllegatiMailOut		OUT		PLS_INTEGER,					-- Se 1 il/i file delle agibilità possono essere allegati alla mail, se 0 no
1276        			IdTemplateRispostaOut	OUT		INTEGER,						-- Id del modello da usare per il pdf di risposta
1277        			URITemplateRispostaOut	OUT		VARCHAR2,						-- URI - in notazione StorageUtil - del modello da usare per il pdf di risposta
1278        			NomeTemplateRispostaOut	OUT		VARCHAR2,						-- Nome del modello da usare per il pdf di risposta
1279        			TipoTemplateOut			OUT		VARCHAR2,						-- Tipo del modello (ai fini di come si iniettano i dati)
1280        			XMLDatiXModelloOut      OUT 	NOCOPY CLOB,					-- XML SezioneCache con i dati da iniettare nel modello
1281        			ContenutoBarcodeOut		OUT		VARCHAR2,						-- Contenuto da mettere nel barcode da appore sul file della risposta
1282        			TestoInChiaroBarcodeOut	OUT		VARCHAR2,						-- Testo in chiaro da mettere vicino al barcode 
1283        			ErrContextOut			OUT		VARCHAR2,						-- Contesto (ovvero package e/o funzione/procedura) in cui si è verificato l'errore; se NULL è la funzione stessa
1284        			ErrCodeOut				OUT		PLS_INTEGER,					-- N.ro errore di uscita
1285        			ErrMsgOut				OUT 	VARCHAR2)						-- Messaggio d'errore
1286        			RETURN PLS_INTEGER;												-- Vale 1 se tutto è andato a buon fine, altrimenti 0
1287        
1288        	/****************funzione per richiedere lo sblocco del limite di agibilità che un utente può richiedere da Portale ***************/
1289        	function RichSbloccoLimAgibilita(
1290        			XMLRequestIn			IN		CLOB,							-- (obblig.) Tag XML sbloccoLimiteRichiesteRequest con i dati della request di sblocco limite richieste
1291        			ErrContextOut			OUT		VARCHAR2,						-- Contesto (ovvero package e/o funzione/procedura) in cui si è verificato l'errore; se NULL è la funzione stessa
1292        			ErrCodeOut				OUT		PLS_INTEGER,					-- N.ro errore di uscita
1293        			ErrMsgOut				OUT 	VARCHAR2)						-- Messaggio d'errore
1294        			RETURN PLS_INTEGER;												-- Vale 1 se la richiesta è stata presa in carico, altrimenti 0
1295        
1296        
1297        
1298        	/****************funzione usata dal WEB SERVICE di GetDetermina che serve a recuperare dati e file di una determina (usato dal comune di Milano) ***************/
1299        	function GetDetermina(
1300        			CodIdConnectionTokenIn	IN		DMT_CONNECTION_TOKEN.CI_CONNECTION_TOKEN%TYPE,
1301        															--(obblig.) Codice identificativo del token di connessione
1302        			XMLRequestIn			IN		CLOB,			--(obblig.) XML in input al Web service di GetDetermina (secondo schema RequestGetDetermina.xsd)
1303        			XmlResponseOut			OUT		NOCOPY CLOB,	-- XML di response del web-service di GetDetermina, restituito anche in caso di errore, conforme a schema ResponseGetDetermina.xsd
1304        			ListaFileOut   			OUT		NOCOPY CLOB,	-- XML conforme a schema LISTA_STD.xsd con URI dei file (solo una colonna) da restituire come attachment in output al servizio (nell'ordine con cui sono restituiti qui)
1305        															-- Per file una sola colonna
1306        															-- 1: URI del file in notazione storageUtil
1307        			ErrContextOut			OUT		VARCHAR2,		--Contesto (ovvero package e/o funzione/procedura) in cui si è verificato l'errore; se NULL è la funzione stessa
1308        			ErrCodeOut				OUT		PLS_INTEGER,	--N.ro errore di uscita
1309        			ErrMsgOut				OUT 	VARCHAR2)		--Messaggio d'errore
1310        			RETURN PLS_INTEGER;								--Vale 1 se tutto è andato a buon fine, altrimenti 0
1311        
1312        
1313          /***************** Funzione che a partire dai livelli ricava l'id uo in rubrica	******/
1314          function GetIdUoInRubricaFromLivelli(IdSpAOOIn				      IN    DMT_SOGGETTI_PROD_AOO.ID_SP_AOO%type,				    --(obblig.) Identificativo del soggetto produttore o AOO
1315        			                                 NriLivelliIn		        IN		DMT_STRUTTURA_ORG.NRI_LIVELLI%TYPE,				      --(obblig.) nri livelli
1316        			                                 IdUoInRubricaOut	     OUT		DMT_STRUTTURA_ORG.ID_UO_IN_RUBRICA%TYPE,		    -- Id. uo in rubrica
1317        			                                 ErrContextOut		     OUT		VARCHAR2,					                              --Contesto (ovvero package e/o funzione/procedura) in cui si è verificato l'errore; se NULL è la funzione stessa
1318        			                                 ErrCodeOut			       OUT		PLS_INTEGER,				                            --N.ro errore di uscita
1319        			                                 ErrMsgOut			       OUT 		VARCHAR2)					                              --Messaggio d'errore
1320        			                                 RETURN PLS_INTEGER;									--Vale 1 se tutto è andato a buon fine, altrimenti 0
1321        
1322          /***************** Funzione che a partire dai livelli ricava l'id uo 	******/
1323          function GetIdUoFromLivelli(IdSpAOOIn				      IN    DMT_SOGGETTI_PROD_AOO.ID_SP_AOO%type,				    --(obblig.) Identificativo del soggetto produttore o AOO
1324        			                        NriLivelliIn		      IN		DMT_STRUTTURA_ORG.NRI_LIVELLI%TYPE,				      --(obblig.) nri livelli
1325        			                        IdUoOut	             OUT		DMT_STRUTTURA_ORG.ID_UO%TYPE,		                -- Id. uo
1326        			                        ErrContextOut		     OUT		VARCHAR2,					                              --Contesto (ovvero package e/o funzione/procedura) in cui si è verificato l'errore; se NULL è la funzione stessa
1327        			                        ErrCodeOut			     OUT		PLS_INTEGER,				                            --N.ro errore di uscita
1328        			                        ErrMsgOut			       OUT 		VARCHAR2)					                              --Messaggio d'errore
1329        			                        RETURN PLS_INTEGER;									--Vale 1 se tutto è andato a buon fine, altrimenti 0
1330        
1331          /***************** Funzione che a partire dal prov_ci_uo ricava l'id uo in rubrica	******/
1332          function GetIdUoInRubricaFromProvCiUo(IdSpAOOIn				      IN    DMT_SOGGETTI_PROD_AOO.ID_SP_AOO%type,				    --(obblig.) Identificativo del soggetto produttore o AOO
1333        			                                  ProvCiUoIn		        IN		DMT_STRUTTURA_ORG.PROV_CI_UO%TYPE,				      --(obblig.) prov ci uo
1334        			                                  IdUoInRubricaOut	   OUT		DMT_STRUTTURA_ORG.ID_UO_IN_RUBRICA%TYPE,		    -- Id. uo in rubrica
1335        			                                  ErrContextOut		     OUT		VARCHAR2,					                              --Contesto (ovvero package e/o funzione/procedura) in cui si è verificato l'errore; se NULL è la funzione stessa
1336        			                                  ErrCodeOut			     OUT		PLS_INTEGER,				                            --N.ro errore di uscita
1337        			                                  ErrMsgOut			       OUT 		VARCHAR2)					                              --Messaggio d'errore
1338        			                                  RETURN PLS_INTEGER;									--Vale 1 se tutto è andato a buon fine, altrimenti 0
1339        
1340          /***************** Funzione che a partire dal prov_ci_uo ricava l'id uo 	******/
1341          function GetIdUoFromProvCiUo(IdSpAOOIn				    IN    DMT_SOGGETTI_PROD_AOO.ID_SP_AOO%type,				    --(obblig.) Identificativo del soggetto produttore o AOO
1342        			                         ProvCiUoIn		        IN		DMT_STRUTTURA_ORG.PROV_CI_UO%TYPE,				      --(obblig.) prov ci uo
1343        			                         IdUoOut	           OUT		DMT_STRUTTURA_ORG.ID_UO%TYPE,		                -- Id. uo 
1344        			                         ErrContextOut		   OUT		VARCHAR2,					                              --Contesto (ovvero package e/o funzione/procedura) in cui si è verificato l'errore; se NULL è la funzione stessa
1345        			                         ErrCodeOut			     OUT		PLS_INTEGER,				                            --N.ro errore di uscita
1346        			                         ErrMsgOut			     OUT 		VARCHAR2)					                              --Messaggio d'errore
1347        			                         RETURN PLS_INTEGER;									--Vale 1 se tutto è andato a buon fine, altrimenti 0
1348        
1349        
1350          /***************** Funzione che a partire dall'id uo ricava l'id uo in rubrica	******/
1351          function GetIdUoInRubricaFromIdUo(IdSpAOOIn				      IN    DMT_SOGGETTI_PROD_AOO.ID_SP_AOO%type,				    --(obblig.) Identificativo del soggetto produttore o AOO
1352        			                              IdUoIn		            IN		DMT_STRUTTURA_ORG.ID_UO%TYPE,				            --(obblig.) Id. uo
1353        			                              IdUoInRubricaOut	   OUT		DMT_STRUTTURA_ORG.ID_UO_IN_RUBRICA%TYPE,		    -- Id. uo in rubrica
1354        			                              ErrContextOut		     OUT		VARCHAR2,					                              --Contesto (ovvero package e/o funzione/procedura) in cui si è verificato l'errore; se NULL è la funzione stessa
1355        			                              ErrCodeOut			     OUT		PLS_INTEGER,				                            --N.ro errore di uscita
1356        			                              ErrMsgOut			       OUT 		VARCHAR2)					                              --Messaggio d'errore
1357        			                              RETURN PLS_INTEGER;									--Vale 1 se tutto è andato a buon fine, altrimenti 0
1358        
1359        
1360          /***************** Funzione che a partire dal prov_ci_classificazione ricava l'id classificazione 	******/
1361          function GetIdClassifFromProvCiClassif(IdPianoClassifIn            IN   DMT_TITOLARIO.ID_PIANO_CLASSIF%type,				  --(obblig.) Identificativo del piano di classificazione
1362        			                                   ProvCiClassificazioneIn		 IN		DMT_TITOLARIO.PROV_CI_CLASSIFICAZIONE%TYPE,	  --(obblig.) prov ci classificazione
1363        			                                   IdClassificazioneOut	       OUT	DMT_TITOLARIO.ID_CLASSIFICAZIONE%TYPE,		    -- Id. classificazione
1364        			                                   ErrContextOut		           OUT	VARCHAR2,					                            -- Contesto (ovvero package e/o funzione/procedura) in cui si è verificato l'errore; se NULL è la funzione stessa
1365        			                                   ErrCodeOut			             OUT	PLS_INTEGER,				                          -- N.ro errore di uscita
1366        			                                   ErrMsgOut			             OUT 	VARCHAR2)					                            -- Messaggio d'errore
1367        			                                   RETURN PLS_INTEGER;									--Vale 1 se tutto è andato a buon fine, altrimenti 0
1368        
1369        	/***************** Funzione che a partire da un xml conforme allo schema TrovaSoggettiRubrica.xsd restituisce i sogggetti presenti in IPA i cui estremi sono indicati nella request ******/
1370        	FUNCTION TrovaSoggettiRubrica(
1371        			CodIdConnectionTokenIn	IN		DMT_CONNECTION_TOKEN.CI_CONNECTION_TOKEN%TYPE,
1372        																			--(obblig.) Codice identificativo del token di connessione
1373        			XMLIn				IN		CLOB,								--(obblig.) XML con tracciato conforme a schema TrovaSoggettiRubrica.xsd
1374        			XmlOut				OUT		NOCOPY CLOB,						-- XML con dati di output specifici del WS, restituito solo in caso di successo, conforme a schema Output_TrovaSoggettiRubrica.xsd
1375        			FlgRollBckFullIn		IN 		PLS_INTEGER DEFAULT NULL,		--(valori 1/0/NULL) Se 1 in caso di errore viene fatta la rollback completa (non a savepoint), altrimenti la rollback delle sole modifiche effettuate nella stored
1376        																			--ATTENZIONE: La rollback, di qualsiasi tipo, non riguarda la scrittura di eventuali log
1377        			FlgAutoCommitIn		IN 		PLS_INTEGER DEFAULT NULL,			--(valori 1/0/NULL) Se vale 1, dopo la rollback e qualunque sia l'esito (positivo o meno), la funzione esegue una commit finale
1378        			ErrContextOut		OUT		VARCHAR2,							--Contesto (ovvero package e/o funzione/procedura) in cui si è verificato l'errore; se NULL è la funzione stessa
1379        			ErrCodeOut			OUT		PLS_INTEGER,						--N.ro errore di uscita
1380        			ErrMsgOut			OUT 	VARCHAR2)							--Messaggio d'errore
1381        			RETURN PLS_INTEGER;												--Vale 1 se tutto è andato a buon fine, altrimenti 0
1382        
1383        END Dmpk_Ws;
